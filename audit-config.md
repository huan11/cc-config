---
description: 扫描配置参数使用方式，识别规范性问题
disable-model-invocation: true
---

扫描当前项目中所有配置参数的使用方式，按来源分类整理，识别配置管理的规范性问题。

## 扫描维度

### 1. 硬编码（Hardcoded）

搜索代码中直接写死的配置值：

- **数据库连接信息**：主机名、端口、库名直接出现在代码里（非配置文件）
- **URL / API 地址**：写死的接口地址、Webhook 地址
- **魔术数字**：无名称的阈值、超时时间、重试次数、分页大小等
- **魔术字符串**：写死的表名、列名、产品代码、路径片段
- **业务常量**：写死的利率、费率、倍数等业务参数

搜索方式：用 Grep 搜索 `http://`、`https://`、`localhost`、`127.0.0.1`、端口号模式 `:\d{4,5}`、常见硬编码模式。逐文件阅读关键业务代码，人工判断是否为应提取的配置。

### 2. 环境变量（Environment Variables）

搜索 `.env` 文件和代码中 `os.environ`、`os.getenv`、`${...}` 引用：

- 列出所有声明的环境变量（`.env` / `.env.example`）
- 列出所有代码中读取的环境变量
- 交叉比对：是否有代码引用了但 `.env` 未声明的变量，或 `.env` 声明了但无人使用的变量

### 3. 配置文件（JSON / YAML / TOML / INI）

扫描所有非依赖管理的配置文件：

- JSON 配置：路径映射、数据库连接、数据源模式等
- YAML 配置：表结构定义、产品列表、因子列表等
- 其他格式：TOML、INI、properties 等

对每个配置文件记录：文件路径、包含的配置项数量、被哪些代码文件引用、加载方式（直接读取 / 通过封装函数）。

### 4. Excel 文件配置

搜索代码中所有 `.xlsx` / `.xls` 引用：

- 配置中声明的 Excel 路径
- 代码中的 `read_excel` / `to_excel` 调用
- 使用到的 sheet 名称
- 当前是否实际生效（是否被 mode 开关绕过）

### 5. 数据库配置（连接串 / 表名映射）

- 数据库连接信息从哪里加载（配置文件 vs 环境变量 vs 硬编码）
- 数据库名、表名在代码中如何引用（通过配置键 vs 直接写死）
- 连接池配置（有无配置、参数是否合理）

### 6. 命令行参数

- 搜索 `argparse`、`sys.argv`、`click` 等命令行参数定义
- 列出所有支持的命令行参数、默认值、用途

## 输出报告

```
## 配置参数审计报告

### 总览

| 配置来源 | 数量 | 状态 |
|---------|------|------|
| 硬编码 | N 处 | ⚠️ 需关注 / ✅ 合理 |
| 环境变量 | N 个 | ... |
| JSON 配置文件 | N 个文件 | ... |
| YAML 配置文件 | N 个文件 | ... |
| Excel 配置 | N 个文件 | ... |
| 命令行参数 | N 个 | ... |

### 1. 硬编码清单

| # | 文件:行号 | 硬编码内容 | 类型 | 风险 | 建议 |
|---|----------|-----------|------|------|------|
| 1 | file.py:42 | `"localhost:3306"` | 数据库地址 | 高 | 移入 .env |

### 2. 环境变量清单

| 变量名 | .env 声明 | .env.example | 代码引用位置 | 用途 |
|--------|----------|-------------|-------------|------|
| DB_USER | ✅ | ✅ | config.json:10 | 数据库用户 |
| API_KEY | ❌ 缺失 | ✅ | app.py:5 | 接口认证 |

### 3. 配置文件清单

| 文件 | 格式 | 配置项数 | 引用模块 | 加载方式 |
|------|------|---------|---------|---------|
| config.json | JSON | 15 | main.py | json.load() |

### 4. Excel 配置清单

| 文件 | 路径来源 | 是否实际生效 | 引用模块 |
|------|---------|------------|---------|

### 5. 数据库连接配置

| 数据库 | Host 来源 | 认证来源 | 表名引用方式 |
|--------|----------|---------|-------------|

### 6. 命令行参数

| 参数 | 默认值 | 必填 | 用途 |
|------|-------|------|------|

---

### 问题清单（按严重程度排序）

| # | 级别 | 问题 | 位置 | 建议 |
|---|------|------|------|------|
| 1 | P0 | 数据库密码硬编码 | file:line | 移入 .env |
| 2 | P1 | 环境变量未声明 | .env | 补充声明 |

### 配置管理改进建议

给出 3-5 条最重要的改进建议，按优先级排序。
```

**注意：只报告真实发现的问题，禁止凑数。如果某个维度没有问题，标注"未发现问题"即可。**
