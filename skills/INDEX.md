# Skills 清单

> 人看的技能索引。每个 skill = `~/.claude/skills/<名>/SKILL.md`,由 Claude 按 `description` **自动触发**,也可 `/名` 手动调用。
> 新增/更新 skill 后在此加一行。最后更新:2026-09-13。

| Skill | 用途 | 何时触发 |
|-------|------|---------|
| **feishu-bitable** | 读写飞书多维表格(Bitable/Base):读表/字段/记录、新增列、批量回填、建视图+过滤;**建库+放置(个人/共享文件夹/知识库去个人化)+删库** | 给了 `feishu.cn/base/...` 链接,或说"多维表格/建个表/加一列/加视图/按 X 过滤/回填/放到共享文件夹" |
| **big-screen** | B 端数字大屏设计 / 评审(展厅、展会、向客户决策者演示的高客单价定制项目) | 做/重做大屏方案,或大屏被客户领导吐槽"不高级/不舒服" |
| **excalidraw-diagram** | 生成 Excalidraw 图 JSON,做"有观点"的可视化(流程/架构/概念) | 要画流程图、架构图、概念示意 |
| **kimi-webbridge** | 用本地 daemon 控制用户真实浏览器(带登录态):导航/点击/输入/读页/截图 | 要操作网页、自动化浏览器任务、抓取需登录的内容 |
| **opc** | Founder's Playbook 分阶段陪跑(想法→MVP→上线→规模化),做结构化 devil's advocate | 带产品/创业/商业想法来求判断、挑刺、拍板(而非写代码) |
| **design-harness** | **把用户手上的散料转成一个能跑的标准 Harness 仓库**:先一次性收料(8 项)+ 出缺口清单,再按六段落地——目录骨架 / 指令系统(角色+目标+任务+验收)/ 业务知识(constitution+SOURCE)/ 环境接入(cli-mcp-浏览器)/ 技能与脚本(含验证器+定时任务)/ 状态台账(STATUS+TODO+progress)。方法论(双层循环、四子系统、5 坏味道)藏在 references/ 当作业依据 | "帮我给这摊活搭套 harness / Agent 系统";"这是我手上的资料,整成 Agent 能用的仓库";"AGENTS.md 怎么写";或既有 harness"时好时坏、说做完其实没做完、规则越加越笨" |

## 约定

- **一个能力域一个 skill**(feishu-bitable 覆盖读/写/列/视图,不按单个操作拆分)。
- **`description` 写触发条件、不写介绍**——它决定会不会被自动加载。
- **沉淀触发器**:每摸通一个磨人的流程,当场沉淀/更新成 skill,不留到下次重来。
- 非技能目录(如 `opc-workspace/` 是 opc 的开发/评测工作区,无 SKILL.md)不计入本清单。
