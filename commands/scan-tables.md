---
description: 表名硬编码扫描与集中化方案
disable-model-invocation: true
---

扫描项目中所有表名和 Schema 引用，生成统一注册表，识别散落的硬编码表名，并给出集中化管理方案。

## 扫描流程

### 1. 扫描配置源（已声明的表名）

自动发现项目中的表名定义来源，常见来源包括但不限于：

- **SQL DDL 文件**（`*.sql`）：解析 `CREATE TABLE` 语句，提取表名、所属 database
- **ORM 模型**（如 SQLAlchemy、Django Model、MyBatis XML）：提取 `__tablename__`、`@Table`、`<mapper>` 等声明
- **配置文件**（`*.yaml`、`*.json`、`*.toml`、`*.ini`）：提取 `table_name`、`to_sql` 目标等表名引用
- **常量/枚举定义**（`.py`、`.java`、`.go` 等）：提取已集中定义的表名常量

先扫描项目结构，识别出本项目实际使用了哪些配置源，然后逐个提取表名。将所有结果合并为"已声明表名集合"。

### 2. 扫描代码引用（业务代码中的表名使用）

在所有业务代码文件中搜索表名硬编码，搜索模式：

- SQL 语句中的表名：`FROM table_name`、`INTO table_name`、`JOIN table_name`、`UPDATE table_name`
- ORM 查询中的表名字符串参数
- 数据库操作 API 中的表名参数（如 `to_sql(name=...)`、`pd.read_sql`、`execute()` 等）
- 字符串字面量中疑似表名的标识符（蛇形命名、与已声明表名匹配的）

对每个发现的表名记录：文件路径、行号、使用方式（读/写/DDL）、上下文代码片段。

### 3. 生成注册表总览

按 database 分组，输出所有发现的表名：

```
## 表名注册总览

### Database: xxx

| # | 表名 | 定义来源 | 引用文件 | 引用方式 | 是否集中管理 |
|---|------|---------|---------|---------|------------|
| 1 | user_account | DDL + ORM | user_service.py:42 | 读+写 | ✅ 已声明 |
| 2 | some_table | ❌ 仅硬编码 | order.py:88 | 读 | ❌ 未管理 |
```

### 4. 问题识别

标记以下三类问题：

**P0 — 未纳入管理的硬编码表名**
- 仅在业务代码中以字符串出现，但未在任何配置文件/ORM/DDL 中声明
- 这些是最需要整改的

**P1 — 重复定义**
- 同一表名在多处配置源中重复定义
- 可能导致不一致

**P2 — 孤儿表名**
- 在配置文件中声明但代码中无任何引用
- 可能是已废弃的表

输出问题清单：

```
## 问题清单

| # | 级别 | 表名 | 问题 | 位置 | 建议 |
|---|------|------|------|------|------|
| 1 | P0 | xxx_table | 仅硬编码，未纳入管理 | file.py:42 | 集中定义 |
| 2 | P1 | yyy_table | 多处重复定义 | a.yaml, b.json | 统一到一处 |
| 3 | P2 | zzz_table | 已声明但无代码引用 | schema.sql | 确认是否废弃 |
```

### 5. 生成集中化管理方案

基于扫描结果和项目的技术栈，给出适合当前项目的表名集中管理方案：

**A. 生成 `global_setting/db_constants.py`**

固定路径 `global_setting/db_constants.py`，作为项目中数据库相关常量的唯一定义处：

```python
"""
数据库常量注册表。
所有数据库名、表名在此集中定义，业务代码统一从此处导入。
禁止在业务代码中硬编码数据库相关字符串。
"""

class DB:
    """Database 名称常量"""
    MAIN = "main_db"
    # ... 基于扫描结果补全

class Table:
    """表名常量。属性名 = 大写蛇形，值 = 实际表名字符串。"""
    USER_ACCOUNT = "user_account"
    ORDER_DETAIL = "order_detail"
    # ... 基于扫描结果补全
```

生成规则：
- 文件路径固定为 `global_setting/db_constants.py`
- 常量名 = 表名大写蛇形（如 `user_account` → `USER_ACCOUNT`）
- 只包含扫描中实际发现的表名/库名，不编造

**B. 修改文件清单**

列出每个需要替换硬编码的业务文件，给出具体的 before → after：

```
## 修改清单

| # | 文件:行号 | 当前写法 | 替换为 | 优先级 |
|---|----------|---------|--------|-------|
| 1 | user_service.py:42 | `"user_account"` | `Table.USER_ACCOUNT` | P0 |
```

**C. 确认后执行**

输出完整方案后，询问用户是否执行修改。用户确认后再执行，不自动执行。

## 守护栏

- **扫描阶段只读不写**：步骤 1-4 只读取文件，不修改任何代码或配置
- **步骤 5 生成文件需确认**：常量文件和业务文件修改需用户确认后才执行
- **不连接数据库**：仅基于代码和配置文件分析，不执行任何 SQL
- **不编造表名**：只包含扫描中实际发现的表名/库名
- **如果某类问题为零，标注"未发现"，禁止凑数**
- **适配项目技术栈**：方案要符合项目现有的语言、框架和目录结构
