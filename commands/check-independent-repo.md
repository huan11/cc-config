---
description: 检查仓库是否满足「独立仓库」标准：输出数据只写数据库、运行所需一切（含配置与凭据）全入 git、新机器装好环境+给 DB 接入信息+pull 即可跑通
argument-hint: [可选: 仓库路径，默认当前工作目录]
---

# 独立仓库达标检查

按三条硬判据逐条判 PASS / FAIL，每条 FAIL 必须给 `文件:行号`。**三条全 PASS 才算达标。**

（通用的"还差多远"用 `/audit-standalone`；本命令只判合不合规。）

## 判据一 · 最终输出数据只写数据库

**只管输出。** 业务产出必须落在数据库，不落文件系统。输入侧不在本判据范围（相关问题归判据三）。

**FAIL**：
- 业务输出写成文件 —— `to_csv|to_excel|to_parquet|to_pickle|np.save|pickle.dump|savefig|open(...,'w')`；MATLAB `save|writetable|xlswrite|csvwrite`
- 输出写到仓外路径 —— 字面量 `/tmp/` `C:\` `D:\` `~/` `\\` 网络盘
- 仓库里提交了输出产物 —— `git ls-files` 筛 `*.csv|xlsx|xls|parquet|pkl|h5|mat|npy|npz|db|sqlite*|feather`，判是模板还是真产出
- 用文件持久化状态 —— 水位、已处理清单、增量游标（应入库）

**灰区**（列出不判 FAIL）：`logs/*.log` 运行日志、失败样本转储（如 `error_batch.csv`）、空壳模板文件。

## 判据二 · 运行所需的一切都在 git

**齐全优先。** 现阶段正在贯通各项目，**配置文件与含凭据的隐私信息也必须入库**——少一样新机器就跑不起来。本判据只问"缺不缺"，不问"该不该明文"。

**FAIL**：
- 运行必需的配置不在仓库 —— `.env` / 连接配置 / 业务配置被 `.gitignore` 排除，或只有 `*.example` 没有实体
- 代码读的配置键在仓内任何文件中都找不到
- `.gitignore` 排除了该入库的东西 —— `.env` / `*.yaml` / `config/` / `docs/`
- 代码引用的相对路径在仓库里不存在
- 依赖仓外共享库 —— `sys.path.append(os.getenv(...))`、`PYTHONPATH` 注入、MATLAB `addpath` 到仓外
- `git status --porcelain` 有未跟踪或已改内容
- （若给了机器 SSH）部署目录与 `git ls-files` 的差集

📋 **附凭据清单**（不判 FAIL）：列出仓内明文凭据的位置与键名（如"`config/db.yaml:22` 有 `password`"），**不回显值**。这是当前阶段有意接受的状态，仅作留档，供日后收敛时按清单处理。

## 判据三 · 装环境 + 给 DB 接入信息 + pull 即可跑通

**FAIL**：
- 首次运行需人工预置数据 —— 代码从本地数据文件读业务输入（`read_csv|read_excel|read_parquet|np.load|loadmat`，读配置/模板不算），而该文件不在仓库
- 依赖声明不全 —— 有 `import` 不在声明里；有 lock 无 `pyproject.toml`；MATLAB 未写版本与 toolbox
- 硬编码绝对路径 —— `D:\` `C:\` `/Users/` `/home/`（含 `.bat` `.ps1` `.sh` 内）
- 硬编码机器标识 —— 主机名 / IP / `MACHINE_ID` 默认值 / `COMPUTERNAME` 分支（**DB host 除外**，那是接入信息）
- 依赖预先存在的数据根、挂载盘、网络路径
- 依赖外部软件却未在文档声明 —— MATLAB / 券商终端 / Excel COM / ODBC
- DB 接入信息散落多处，无单一事实源（导致"给一份接入信息"无法生效）
- 缺环境与启动文档，或文档命令与实际不符

## 铁律

一切实拉、结论必带 `文件:行号`；禁止回显凭据值；不改代码只给建议；同类超 15 条列前 15 条并写明省略数；灰区标 🟡 交人确认，拿不到写 `🟡 未取到` + 原因。

## 输出

```
# <仓库> 独立仓库达标检查

| 仓库 | <路径> <branch>@<sha> ｜ 扫描 <N> 文件 ｜ **结论 ✅ 达标 / 🔴 不达标（<M> 条 FAIL）** |

| 判据 | 判定 | FAIL | 灰区 |
|---|---|---:|---:|
| 一 输出只写库 | | | |
| 二 运行所需全入 git | | | |
| 三 装环境即可跑 | | | |

## 判据一 / 二 / 三（各一节）
| # | 反模式 | 证据 文件:行号 | 整改 |
🟡 灰区项；判据二另附 📋 凭据清单

## 从 clone 到跑通的实际差距
按顺序列新机器必做事项，标出**超出「装环境 + DB 接入信息」**的——每项都是判据三的 FAIL 证据。

## 整改清单
按「阻断性 × 改动量」排序：改哪个文件、改成什么、影响哪些入口。

## 结论
一句话：是不是独立仓库；不是的话最先改哪三条。
```

**判定**：三条全 PASS = ✅ 达标；任一 FAIL = 🔴 不达标。不设中间等级。
