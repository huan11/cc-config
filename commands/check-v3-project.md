# check-v3-project

对一个 v3 代码库执行本地运行验证 & 文档化，最终更新 `/Users/asher/Documents/code3.0/本地运行命令.md`。

## 输入

$ARGUMENTS — 项目路径（如 `/Users/asher/Documents/code3.0/v3-xxx`），为空则使用当前工作目录。

## Checklist

按顺序逐项执行，每完成一项标记 ✅，遇到问题标记 ❌ 并说明原因。

### 1. 摸清项目

- [ ] 读项目入口文件，确认启动命令
- [ ] 读配置文件，确认输入（数据源）和输出（写入目标）
- [ ] 确认运行频率（cron / 手动 / 事件触发）
- [ ] 确认生产部署位置（哪台服务器、代码路径）

### 2. 本地跑通

- [ ] 安装依赖（`uv sync`）
- [ ] 跑测试（`uv run pytest tests`）
- [ ] 本地 dry-run / plan-only 验证能跑起来
- [ ] 本地 dry-run 连真实数据源，确认有数据返回

### 3. 环境配置规范化

- [ ] 拆分 `.env.dev`（本地）/ `.env.prod`（生产），入库
- [ ] 代码中通过 `APP_ENV` 环境变量切换配置（默认 `dev`）
- [ ] 生产 cron 脚本中 `export APP_ENV=prod`
- [ ] `.gitignore` 排除 `.env`，保留 `.env.dev` / `.env.prod`

### 4. 飞书通知可读性

- [ ] 通知包含运行时间
- [ ] 通知包含模式（dry-run / 真实写入）
- [ ] 通知包含每个任务的类型和行数明细
- [ ] 通知包含成功/失败汇总
- [ ] 确认 webhook 地址统一到目标群

### 5. 更新文档 `本地运行命令.md`

打开 `/Users/asher/Documents/code3.0/本地运行命令.md`，在文件末尾新增该项目章节，包含：

- [ ] 基本信息（入口、部署位置、运行频率、日志路径、飞书通知链接、环境切换方式）
- [ ] 配置文件说明（dev/prod 差异、切换方式）
- [ ] 生产自动化（cron → 脚本 → 入口的完整调用链）
- [ ] 输入（数据源：主机、库名、用途）
- [ ] 输出（目标表 + 任务类型对应关系 + 典型行数，基于实际运行结果）
- [ ] 本地运行命令（安装、测试、plan-only、dry-run、execute）
- [ ] 最近一次运行记录

### 6. 提交部署

- [ ] 提交代码并 push
- [ ] 目标服务器 git pull 同步
- [ ] 生产环境 dry-run 验证
- [ ] 飞书群确认收到通知

## 执行规则

- 每完成一个大步骤（1~6），向孟老板汇报进度
- 遇到需要确认的决策（如 webhook 地址、部署服务器），先问再做
- 每一步都要实际运行验证，不能只看代码猜测
- 参考 v3-mktdata-update 在 `本地运行命令.md` 中的格式保持一致
