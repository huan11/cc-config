分析前端所有模块调用的后端 API，生成完整的 API 调用地图。

## 分析流程

### 1. 扫描 API 定义层

读取 `packages/api/src/domains/` 下所有文件，提取已封装的 API 端点（方法、路径、说明）。

### 2. 扫描应用级 API 封装

分别扫描 `apps/web/lib/api/` 和 `apps/admin/lib/api/` 目录下所有 `.ts` 文件，提取：
- 每个文件封装的 API 端点（HTTP 方法 + URL 路径）
- 函数名和用途说明

### 3. 扫描页面级 API 调用

在 `apps/web/app/` 和 `apps/admin/app/` 中搜索以下模式，找出所有直接调用 API 的位置：
- `apiClient.get|post|put|delete|patch(`
- `api.` 开头的域 API 调用（如 `api.fund.list()`）
- `fetch(` 直接调用
- SSE / EventSource 调用

对每个调用点记录：
- 所属页面模块（根据目录路径判断）
- 调用的 API 端点
- HTTP 方法
- 用途

### 4. 生成报告

按以下格式输出结果：

```
## API 调用地图

### [应用名] — [模块名]

| 页面/组件 | HTTP 方法 | API 端点 | 说明 |
|-----------|----------|---------|------|
| ...       | GET      | /api/v1/... | ... |
```

**分组规则：**
- 第一层按应用分（Web / Admin）
- 第二层按功能模块分（认证、基金、报告、资讯等）
- 每个模块内按页面/组件列出所有 API 调用

**最后附加：**
- 统计信息：各模块 API 数量、总计
- 两个应用共用的 API 端点（如有）
- 只在 API 封装层定义但未被页面调用的"死" API（如有）

### 5. 输出方式

将完整报告输出为 Markdown 表格，直接展示给用户。不写入文件，除非用户要求保存。
