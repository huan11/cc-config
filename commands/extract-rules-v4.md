---
description: 从当前项目代码库逐文件穷举业务规则，生成可长期维护的三层业务规则文档（说明书 + 取证底稿 + 追溯矩阵）
argument-hint: [--verify-only]
---

从当前项目代码库中逐文件精读，穷举提取全部业务规则，生成三份独立但互相可追溯的文档：

1. `business-rules.md` — 业务说明书，让人不看代码就能读懂系统
2. `business-rules-evidence.md` — 逐文件取证底稿，证明"这不是编的"
3. `br-inventory.md` — 规则索引与追溯矩阵，做覆盖率和反向检索

三份文件必须一起产出、一起验证、一起更新。任何一份缺失，任务不算完成。

## 输入参数

- `codebase`: 当前工作目录（自动获取）
- `--verify-only`: 跳过全量提取，对已有三份文档做六项校验 + 增量补全（会修改文档，但不整体重写）

输出固定写入 `docs/` 目录。

## 基本原则

1. **逐文件，不跳过** — 只要是包含业务逻辑的源文件，就必须完整阅读
2. **提取，不编造** — 规则必须来自代码事实；解释可以有，但必须能追溯回代码
3. **三层分离** — 主文档讲业务，底稿讲证据，矩阵讲映射，职责不混写
4. **主链优先，遗留隔离** — 当前生效规则进主文档，遗留/旁支/死代码只进底稿
5. **编号稳定** — 已有 BR-XX 和 INV-XXXX 默认保留，新增追加，废弃标记不回收
6. **代码优先** — 文档间出现冲突时以源码为准，修正文档而非臆造代码意图

## 术语与判定

### 业务源文件

满足以下任一条件的文件即为业务源文件：

- 包含业务条件判断（if/else、switch、匹配分支）
- 包含业务计算公式、阈值、取整、权重、时序窗口
- 包含数据过滤、分类、映射、排除逻辑
- 包含表/文件/接口的业务路由选择
- 包含核心流程的执行顺序编排

必须检查的文件类型：`.py`、`.m`、`.sql`、`.sh`、`.bat`、`.yaml`/`.yml`、`.json`

配置文件只有包含业务路由、阈值、品类映射等业务含义时才算；纯日志级别、依赖列表、格式化配置不算。

### 文件状态

每个候选文件必须归入以下四类之一，不得处于"未分类"状态：

| 状态 | 含义 | 后续处理 |
|------|------|---------|
| `ACTIVE_CORE` | 当前主链生效的核心业务文件 | 完整提取证据，规则进主文档 |
| `LEGACY_CORE` | 包含业务逻辑但不在当前主链 | 完整提取证据，规则只进底稿遗留区 |
| `SUPPORTING_BUSINESS` | 有少量业务含义，不独立承载主规则 | 检查并按需提取，无规则则标"已检查" |
| `EXCLUDED` | 确认不属于提取范围 | 记录排除理由 |

### 原子业务规则

一条可编号的业务规则应只表达一个业务决策：

- "15:00 前取当天，15:00 后取下一交易日" — 一条规则
- "金额按权重分配后按 100 股取整" — 一条规则
- "保存组合后才生成订单" — 一条规则

不要把多个不同判断塞进同一个子规则。

### 代码事实 vs 规则解释

- **代码事实**：代码明确写出的判断、公式、阈值、顺序、表名、字段名
- **规则解释**：把代码事实翻译成业务可读语言后的结论

底稿必须同时保留这两层。

---

## 执行流程

### Phase 0: 建立判定基线

正式扫描前，先明确并记录：

1. 本次扫描的根路径
2. 扫描的文件类型范围
3. 主链判定依据（哪些入口是当前生效的，依据是什么）
4. **缺失上下文** — 代码引用了外部 Excel/YAML/JSON/Mat/数据库表，但仓库中没有实体

对缺失上下文：只能记录"代码如何消费这些外部输入"，不能臆造其具体内容。

### Phase 1: 全量扫描与分类

遍历目录树，将每个候选文件分类为四种状态之一。

**排除规则（必须写理由）：**
- `tests/`、`docs/`、`__pycache__/`、`*.pyc`
- 纯日志配置、第三方库镜像、自动生成文件
- 备份/副本文件：文件名含"副本"、`copy`，或匹配 `*_ori.py`、`*-backup.*`、`*.old`（词边界匹配，不误伤 optimizer、history 等合法文件），或位于 `backup/`、`bak/`、`archive/` 目录

**扫描输出模板：**

```markdown
## 扫描清单

路径: [当前工作目录]
主链判定: [依据说明]

### ACTIVE_CORE
1. Tracking/running_main.py — 主循环与时段控制 — 入口
2. Tracking/data/data_prepared.py — 数据准备 — 数据准备
3. Optimizer/matlab/optimizer_func_v2.m — 优化求解核心 — 核心计算

### LEGACY_CORE
1. Optimizer/trading_update_auto.py — 旧入口，不属于当前主链

### SUPPORTING_BUSINESS
1. global_dic.py — 提供业务路由和路径映射

### EXCLUDED（含理由）
- tests/test_tracking.py — 测试代码
- data_prepared - 副本.py — 明确备份文件

### 缺失上下文
- config_project/*.yaml — 数据库连接配置，仓库中不含实际连接串
- external Excel files — 代码引用但仓库中无实体
```

扫描清单完成后，等待用户确认再进入 Phase 2。如用户对分类有异议，先修正再继续。

### Phase 2: 逐文件精读 → 产出取证底稿

对 `ACTIVE_CORE` 和 `LEGACY_CORE` 文件逐个完整阅读，提取证据。`SUPPORTING_BUSINESS` 按需提取，若无规则须标明"已检查，无独立业务规则"。

**证据提取对象：**

| 提取对象 | 成为证据的条件 |
|---------|--------------|
| 条件分支 | 基于业务条件进入不同路径 |
| 计算公式 | 影响业务结果的数值运算 |
| 数据过滤 | 保留、排除、归类、分层 |
| 校验规则 | 抛错、告警、阻断条件 |
| 常量阈值 | 业务阈值、白名单、黑名单、窗口 |
| 数据源选择 | 根据条件读取不同表/文件/接口 |
| 输出定义 | 输出表、输出列、枚举映射、文件格式 |
| 执行顺序 | 先后依赖、串行/并行关系 |
| 特殊处理 | 硬编码覆盖、例外路径、补丁逻辑 |

**每个文件的证据格式：**

```markdown
## 文件: Tracking/running_main.py

文件状态: ACTIVE_CORE
文件角色: Tracking 模块主循环与时段控制

| # | 行号 | 代码事实 | 规则解释 | 分类 | 主链状态 |
|---|------|---------|---------|------|---------|
| 1 | 55-60 | `hour==9 and minute<35` 下清空多张 `realtime_*` 表 | 开盘前 9:00-9:34 清空实时结果表 | 时段控制 | current |
| 2 | 65-67 | `hour >= 16` 调用 `historySql_saving` | 收盘后归档 realtime 数据为历史数据 | 收盘处理 | current |
| 3 | 214 | `time.sleep(30)` | 实时主循环每 30 秒执行一次 | 执行频率 | current |
```

**底稿末尾独立 section（不混入主文档）：**
- 遗留/非主链代码说明
- 已知问题与潜在 Bug
- 排除文件说明
- 缺失上下文与外部依赖

### Phase 3: 按业务域组织 → 产出业务说明书

从证据中把 `ACTIVE_CORE` 的规则按业务域整合为主文档。目标不是"逐文件展示"，而是"按业务域解释当前系统如何工作"。

**主文档约束：**
- 只写当前生效规则
- 遗留逻辑、死代码、注释掉的路径不进正文
- 每条规则都必须有 evidence 支撑
- 同一业务域内的规则不要互相矛盾
- 同一规则不要在多个 BR 中重复

**BR section 模板：**

```markdown
## BR-07: Tracking 实时初始化

目的:
- 在开盘前清空上一轮实时结果，保证当日实时计算从干净状态开始

### BR-07a: 开盘前清空实时表

触发条件:
- 当前时间满足 `hour == 9` 且 `minute < 35`

处理规则:
1. 系统进入开盘前初始化窗口
2. 清空以下实时表：realtime_portfolioreturn、realtime_scoresplit 等 7 张表
3. 后续实时结果在清空后的表中重新写入

边界与例外:
- 9:35 及以后不再执行该清理逻辑
- 只影响 realtime 表，不影响历史表

输出:
- 当日 realtime 表被初始化为空

来源:
- Tracking/running_main.py:55-60
- Tracking/running_main.py:93-99
```

**模板使用规则：**
- **"目的"必填** — 每个 BR section 开头必须说明这个域解决什么业务问题
- 子规则的"触发条件/输入/处理规则/边界与例外/输出"按需使用，不强制全填
- **"来源"始终压到每节末尾**，不在规则描述中穿插行号

**公式与例子：**
- 非平凡公式应给数值例子
- 源码中有注释示例或测试数据 → 标注 `例(源码)`
- 源码无示例但确有必要 → 标注 `例(说明性)`，不得伪装为源码事实
- 非常简单的公式不强制配例子，不要为凑模板制造低价值样例

**主文档建议结构：**

```markdown
# [系统名称] 业务规则说明书

> 最后更新: YYYY-MM-DD
> 取证底稿: [business-rules-evidence.md](business-rules-evidence.md)
> 追溯矩阵: [br-inventory.md](br-inventory.md)

## 系统概览
[系统目标、核心模块、日常执行时序、ASCII 数据流图]

## BR-01: ...
## BR-02: ...

## 附录 A: 数据依赖汇总
## 附录 B: 常量与阈值
## 附录 C: 缺失上下文说明
```

### Phase 4: 生成追溯矩阵

从 Phase 2 每条证据生成一条 INV 记录，建立 evidence → BR 的映射。

**INV 状态（必填）：**

| 状态 | 含义 |
|------|------|
| `mapped-active` | 已映射到当前生效 BR |
| `mapped-legacy` | 已映射到遗留说明，不进主文档 |
| `intentionally-unmapped` | 确认不应进入 BR，保留在 evidence |
| `pending-review` | 暂未确认，需人工回查 |

**Inventory 模板：**

```markdown
# BR Inventory 追溯矩阵

> 生成时间: YYYY-MM-DD
> 证据来源: [business-rules-evidence.md](business-rules-evidence.md)
> 规则文档: [business-rules.md](business-rules.md)

## 追溯表

| INV ID | 来源文件:行号 | 规则摘要 | 映射目标 | 状态 |
|--------|-------------|---------|---------|------|
| INV-0001 | Tracking/running_main.py:55-60 | 9:00-9:34 清空多张 realtime 表 | BR-07a | mapped-active |
| INV-0002 | Optimizer/trading_update_auto.py:12-28 | 旧入口的 22:30 切日逻辑 | LEGACY-01 | mapped-legacy |
| INV-0003 | Mktdata_update.py:494 | 8*/43* 开头永久不可交易 | — | intentionally-unmapped |

## 覆盖率统计

| 维度 | 数量 |
|------|------|
| 证据总数 | N |
| mapped-active | A |
| mapped-legacy | B |
| intentionally-unmapped | C |
| pending-review | D |
| 主文档 BR 条目 | K |
| 有 evidence 支撑的 BR | J |
```

**编号稳定规则：**
- 已有 BR-XX 和 INV-XXXX 默认保留，新增规则追加到对应域末尾
- 废弃规则标记 `deprecated`，编号不回收不重排
- `--verify-only` 模式禁止全量重排编号

### Phase 5: 六项校验

**这一步不可跳过。** 完成初稿后，重新逐文件回查，并进行以下六项校验：

| # | 检查方向 | 发现什么问题 | 处置 |
|---|---------|------------|------|
| 1 | 代码 → evidence | 代码有业务逻辑但底稿没记录 | 补入 evidence |
| 2 | evidence → inventory | 有证据但没生成 INV | 补入 inventory |
| 3 | inventory → BR | 有 INV 但没映射到主文档或遗留说明 | 归类或标 intentionally-unmapped |
| 4 | BR → evidence | 主文档有规则但找不到证据支撑 | 补 evidence 或删除该 BR |
| 5 | 文档内部一致性 | 同域规则矛盾、前后表述不一致、三份文档对同一事实冲突 | 回查代码，修正文档 |
| 6 | 范围与主链边界 | 漏扫非 Python 业务文件、遗留混入主文档、当前规则误归遗留 | 修正分类和归属 |

**验证输出模板：**

```markdown
## 交叉验证结果

### 补充项
| # | 检查方向 | 发现 | 处置 |
|---|---------|------|------|
| 1 | 代码→evidence | running_main.py:214 的 sleep(30) 未提取 | 补入 evidence 并生成 INV |
| 2 | 文档一致性 | BR-11h 写"回溯 3 天"但 evidence 显示只保留 1 工作日 | 回查代码后修正主文档 |
| 3 | 范围边界 | optimizer_func_v2.m 未进扫描清单 | 纳入 ACTIVE_CORE 并补全证据 |

### 覆盖率
- 扫描业务源文件: N（已全部分类）
- evidence 条目: A → 全部编入 inventory
- mapped-active: B / mapped-legacy: C / intentionally-unmapped: D / pending-review: E
- 主文档 BR 条目: K → 全部有 evidence 支撑
- 内部矛盾: 0 处（已修正）
- 遗留混入: 0 处（已清理）

验证结论: 补充 X 条，修正矛盾 Y 处，清理遗留混入 Z 处
```

### Phase 6: 归档旧版 → 写入产出物

**归档（如目标目录已有同名文件）：**
1. 将已有文件复制到 `docs/archive/` 子目录
2. 文件名加时间戳后缀：`business-rules_2026-03-14_1830.md`
3. 归档完成后再写入新文件

**写入** `docs/` 目录下的三个文件：

1. `business-rules.md`
2. `business-rules-evidence.md`
3. `br-inventory.md`

三份文件之间必须互相链接。

---

## `--verify-only` 模式

当三份文档已存在，执行增量验证和修补：

1. 读取现有三份文档，保留已有 BR/INV 编号
2. 重新逐文件阅读源码
3. 做六项校验
4. 原位补充遗漏项，输出验证结论和增量修改摘要

`--verify-only` **不应导致**：
- 大范围章节重排
- 已有编号无意义变化
- 仅因措辞优化而重写全文

---

## 质量检查清单

### 扫描范围
- [ ] 全部业务源文件已纳入扫描，不限语言
- [ ] 每个候选文件已分类为四种状态之一
- [ ] 排除文件有明确理由
- [ ] 缺失上下文已记录，且没有臆造内容

### business-rules-evidence.md
- [ ] 每个 ACTIVE_CORE 文件有证据表
- [ ] 每个 LEGACY_CORE 文件有证据表或"已检查，无独立规则"说明
- [ ] 每条证据有行号、代码事实、规则解释、分类、主链状态
- [ ] 遗留说明、已知问题、排除文件、缺失上下文在独立 section

### br-inventory.md
- [ ] 每条 evidence 对应一个稳定的 INV ID
- [ ] 每条 INV 有状态（mapped-active / mapped-legacy / intentionally-unmapped / pending-review）
- [ ] 未映射条目已说明原因
- [ ] 覆盖率数字前后一致

### business-rules.md
- [ ] 只包含当前生效规则
- [ ] 每个 BR 有清晰目的（必填）
- [ ] 每个 BR 子规则都能找到 evidence 支撑
- [ ] 关键公式在必要时提供例子，且区分 `例(源码)` / `例(说明性)`
- [ ] 不看代码也能理解规则

### 一致性与维护性
- [ ] 三份文档对同一规则没有冲突
- [ ] 没有遗留/旁支逻辑混入主文档
- [ ] 已有 BR-XX 和 INV-XXXX 编号保持稳定
- [ ] 新增规则通过追加编号接入，不重排全文

## 守护栏

- **不跳文件** — 只要是业务源文件就必须完整阅读
- **不编造** — 只提取代码中实际存在的逻辑
- **不混层** — 主文档讲业务，底稿讲证据，矩阵讲映射
- **不伪装遗留** — 遗留代码不进主文档，当前规则不误归遗留
- **不脑补缺失** — 仓库外的外部依赖只记录消费方式，不臆造内容
- **不重排编号** — 已有编号只增不删不改，废弃标记而非回收
- **六项校验不可跳过** — 这是从"大概率完整"到可信结论的关键步骤
- **不确定就问** — 判断不了文件状态（ACTIVE vs LEGACY）或规则归属时，暂停询问用户，不自行猜测
- **保持人间清醒** — 对"零遗漏"保持谨慎，只在完成校验后给出结论
