---
description: 快速扫描代码仓库，将每个文件分类为四种状态之一，建立后续规则提取的扫描基线。独立可用于快速了解陌生仓库结构。
---

遍历当前工作目录的文件树，将每个候选文件归入四种状态之一，产出一份扫描清单供人工确认。

这是规则提取流程的第一步，但也可以独立使用——快速了解一个仓库"有几块地要扫"。

## 文件状态定义

| 状态 | 含义 | 后续处理 |
|------|------|---------|
| `ACTIVE_CORE` | 当前主链生效的核心业务文件 | `/biz:extract` 完整提取 |
| `LEGACY_CORE` | 包含业务逻辑但不在当前主链 | `/biz:extract` 提取到遗留区 |
| `SUPPORTING_BUSINESS` | 有少量业务含义，不独立承载主规则 | `/biz:extract` 按需检查 |
| `EXCLUDED` | 不属于提取范围 | 记录排除理由 |

每个候选文件必须归入以上之一，不得"未分类"。

## 业务源文件判定

满足以下任一条件即为候选业务文件：

- 包含业务条件判断（if/else、switch、匹配分支）
- 包含业务计算公式、阈值、取整、权重、时序窗口
- 包含数据过滤、分类、映射、排除逻辑
- 包含表/文件/接口的业务路由选择
- 包含核心流程的执行顺序编排

检查范围：`.py`、`.m`、`.sql`、`.sh`、`.bat`、`.yaml`/`.yml`、`.json`

配置文件只有包含业务路由、阈值、品类映射等业务含义时才算。

## 排除规则

以下直接排除，但必须写理由：

- `tests/`、`docs/`、`__pycache__/`、`*.pyc`、`node_modules/`
- 纯日志配置、第三方库镜像、自动生成文件
- 备份/副本文件：文件名含"副本"、`copy`，或匹配 `*_ori.py`、`*-backup.*`、`*.old`（词边界匹配，不误伤 optimizer、history 等合法文件），或位于 `backup/`、`bak/`、`archive/` 目录

## 执行流程

1. 列出目录树
2. 逐文件判定状态
3. 识别主链入口和判定依据
4. 记录缺失上下文（代码引用但仓库中没有的外部依赖）
5. 产出扫描清单
6. **等待用户确认**

## 产出物

写入 `docs/scan-manifest.md`：

```markdown
# 代码仓库扫描清单

> 扫描路径: [当前工作目录]
> 扫描日期: YYYY-MM-DD
> 主链判定依据: [说明]

## ACTIVE_CORE

| # | 文件路径 | 角色简述 | 模块 |
|---|---------|---------|------|
| 1 | Tracking/running_main.py | 主循环与时段控制 | Tracking |
| 2 | Tracking/data/data_prepared.py | 数据准备 | Tracking |

## LEGACY_CORE

| # | 文件路径 | 角色简述 | 不在主链的原因 |
|---|---------|---------|--------------|
| 1 | Optimizer/trading_update_auto.py | 旧入口 | 已被 running_main.py 替代 |

## SUPPORTING_BUSINESS

| # | 文件路径 | 角色简述 |
|---|---------|---------|
| 1 | global_dic.py | 业务路由和路径映射 |

## EXCLUDED

| # | 文件路径/模式 | 排除理由 |
|---|-------------|---------|
| 1 | tests/ | 测试代码 |
| 2 | data_prepared - 副本.py | 备份文件 |

## 缺失上下文

| # | 引用方式 | 说明 |
|---|---------|------|
| 1 | config_project/*.yaml | 数据库连接配置，仓库中无实际连接串 |
| 2 | external Excel files | 代码引用但仓库中无实体 |

## 统计

| 状态 | 数量 |
|------|------|
| ACTIVE_CORE | N |
| LEGACY_CORE | N |
| SUPPORTING_BUSINESS | N |
| EXCLUDED | N |
| 缺失上下文 | N |
```

产出后展示给用户，等待确认。用户对分类有异议，先修正再结束。

## 守护栏

- **不读文件内容** — 这一步只看文件名、路径、目录结构来分类。快速扫描，不深入
- **不确定就标 SUPPORTING_BUSINESS** — 宁可多扫不漏扫，让 `/biz:extract` 去判断
- **主链判定必须有依据** — 不能凭感觉，要说出"为什么这个是当前入口"
- **必须等用户确认** — 分类错了后面全白干，这一步不能跳过确认
