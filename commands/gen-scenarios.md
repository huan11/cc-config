---
description: 从业务规则生成 Given/When/Then 功能测试场景
argument-hint: <业务规则来源：文件路径/BR编号/直接描述> 例: docs/business-rules.md / "BR-06" / "权重和校验"
---

从业务规则（BR）生成结构化的 Given/When/Then 功能测试场景，每个场景标注 Happy Case 或 Sad Case。

## 输入参数

- **source**：`$ARGUMENTS`（可以是 BR 文档路径、具体 BR 编号、或直接描述的业务规则）
- 如果未提供，询问用户提供业务规则来源

## 前置信息收集

| 信息 | 必要性 | 缺失时处理 |
|------|--------|-----------|
| 业务规则（BR 文档或描述） | 必须 | 无法生成场景，要求用户提供 |
| 项目技术栈 | 建议 | 影响场景的具体写法，询问用户 |
| 已有测试场景 | 可选 | 无则从头编写，有则补充，避免编号冲突 |
| 领域术语表 | 可选 | 从 BR 文档中推断，不确定就问 |

## 执行流程

### 第 1 步：理解业务规则

读取输入的 BR，确保理解每条规则的：
- 业务含义（做什么判断/计算）
- 输入和输出（什么数据进，什么结果出）
- 边界条件（零值、空值、极端值、临界值）
- 异常路径（输入非法、数据缺失、状态异常）

如果 BR 描述不够清晰，先问用户澄清，不猜测。

### 第 2 步：编写 GWT 场景

对每条 BR，编写覆盖完整的 Given/When/Then 场景，每个场景**必须标注类型**：

| 类型 | 含义 | 说明 |
|------|------|------|
| **Happy** | 正常路径 | 输入合法，业务按预期执行，产出正确结果 |
| **Sad** | 异常/边界路径 | 输入非法、边界值、异常状态，系统应正确拒绝或降级处理 |

**覆盖要求**：
- 每条 BR 至少 1 个 Happy Case
- 每条 BR 至少 1 个 Sad Case（除非该规则确实没有异常路径，需说明理由）
- 边界值必须覆盖（零值、空值、极大值、临界值等）

### 第 3 步：标注依赖关系

检查场景之间是否存在前置依赖（如 FT-03 的前置状态依赖 FT-02 的输出），用依赖图标注。

### 第 4 步：输出场景文档

按下方格式输出，确认后写入项目文件。

## 输出格式

```markdown
# [项目名称] 功能测试场景

> 来源: [BR 文档路径/规则描述]
> 创建时间: YYYY-MM-DD
> 场景总数: N 个（Happy: N / Sad: N）

---

## BR-XX: [业务规则名称]

### FT-XX-01: [场景名称] [Happy]
  给定: [前置条件/输入数据]
  当:   [触发动作]
  那么: [期望结果，必须可验证]

### FT-XX-02: [场景名称] [Sad]
  给定: [异常/边界条件]
  当:   [触发动作]
  那么: [系统如何处理：拒绝/报错/降级/跳过]

---

## 场景依赖关系

FT-01-01 → FT-03-01（FT-01 的输出是 FT-03 的前置状态）
FT-04-01（独立，无依赖）

---

## 覆盖度统计

| BR 编号 | 名称 | Happy | Sad | 合计 |
|---------|------|-------|-----|------|
| BR-01   | ...  | 2     | 1   | 3    |
| BR-02   | ...  | 1     | 2   | 3    |
| 合计    |      | N     | N   | N    |
```

## 示例

```markdown
## BR-06: 目标持仓计算

### FT-06-01: 标准持仓计算 [Happy]
  给定: stock_money = 10,000,000，权重 = {600519: 0.05}，收盘价 = {600519: 1680.0}
  当:   计算目标持仓
  那么: 目标金额 = 500,000，原始股数 = 297.6，取整后 = 300 股

### FT-06-02: 收盘价为零跳过该股票 [Sad]
  给定: 权重 = {600519: 0.05, 000001: 0.03}，收盘价 = {600519: 1680.0, 000001: 0.0}
  当:   计算目标持仓
  那么: 结果只含 600519，000001 被跳过（除零保护）

### FT-06-03: 收盘价缺失跳过该股票 [Sad]
  给定: 权重含 999999 但收盘价表中无此代码
  当:   计算目标持仓
  那么: 999999 不在结果中（缺失数据保护）
```

## 与其他命令的关系

```
/extract-rules  → 从旧代码穷举提取业务规则（零知识丢失文档）
/gen-scenarios  → 从业务规则生成 GWT 功能测试场景
```

**典型工作流：**
1. `/extract-rules /path/to/old-system` → 产出 `docs/business-rules.md`
2. `/gen-scenarios docs/business-rules.md` → 产出功能测试场景 spec
3. TDD 实现 → 每个场景对应一个测试 → 代码

## 质量检查清单

输出前逐项检查：

- [ ] 每个场景明确标注了 `[Happy]` 或 `[Sad]`
- [ ] 每条 BR 至少有 1 个 Happy Case 和 1 个 Sad Case
- [ ] Given 描述了完整的前置状态（不遗漏关键输入）
- [ ] Then 的结果是可观测、可验证的（有具体数值或明确状态）
- [ ] 边界值已覆盖（零值、空值、极大值、临界值）
- [ ] 场景之间的依赖关系已标注
- [ ] 场景编号连续且无冲突
- [ ] 无重复场景（两个场景测同一件事）

## 守护栏

- 不凭空编造业务逻辑，从 BR 文档中提取，不确定就问
- Given/When/Then 不混入技术实现细节（如"调用 XX 接口"、"INSERT INTO"）
- Then 必须是可验证的断言，不能是"系统正确处理"这种模糊表述
- 如果 BR 描述不足以生成完整场景，明确标注哪些场景可能遗漏，而非猜测
- 保持人间清醒，不懂就问
