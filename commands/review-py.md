审查 Python 模块，按 7 大类 40+ 检查项逐项检查，输出问题清单。

参数：`/review-py <file.py> [depth] [--focus cat1,cat2] [--fix] [--diff [ref]]`

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `<file.py>` | 必填 | 目标文件路径 |
| `depth` | `1` | 下游调用链深度：`1/2/3/all`。只展开项目内 .py 文件，跳过标准库和第三方包 |
| `--focus` | 全部 | 只跑指定类别：`call,error,data,sql,concurrency,api,test` |
| `--fix` | 关闭 | 审查完自动进入 `/fix` 流程（只处理 P0+P1） |
| `--diff` | 关闭 | 只审查 git diff 涉及的改动行及上下文，可指定 ref（如 `--diff HEAD~1`） |

## 第一步：文件识别

读取目标文件，识别类型并标记特殊属性：

| 类型 | 识别方式 | 额外检查 |
|------|----------|----------|
| Flask 路由 | 含 `@app.route` 或 `Blueprint` | 并发控制、参数校验、错误响应 |
| 数据处理 | 含 pandas / DB 操作 | 数据质量、幂等性、DELETE 范围 |
| 执行器 | 含 subprocess / engine.start | 进程生命周期、资源泄漏 |
| 配置 | 含 os.environ / config class | 声明 vs 使用一致性 |
| 测试 | 含 pytest / unittest | 覆盖率、断言与实现是否同步 |

## 第二步：调用链展开（depth>=2 时）

1. 识别目标文件中 import 的所有项目内模块
2. 在项目目录内（排除 `.venv/`、`tests/`）找到的才展开
3. 找不到的视为标准库/第三方包，跳过
4. 按 depth 递归展开，记录调用链树

## 第三步：逐类检查

对目标文件（及展开的子文件）按以下 7 类逐项检查。根据文件内容智能筛选：含 `cursor.execute` 或 SQL 字符串才检查 sql 类，含 `Thread`/`Semaphore`/`subprocess` 才检查 concurrency 类，含 `@app.route` 才检查 api 类，不适用的标 N/A。

### 类别 1：调用点和边界条件（call）

| # | 检查项 | 说明 |
|---|--------|------|
| 1.1 | 函数签名一致 | 调用方与被调用方参数数量、类型、顺序匹配 |
| 1.2 | 可选参数有安全默认值 | `def f(x, mode='safe')` — 默认值不能导致数据错误 |
| 1.3 | 空输入保护 | `None`、空字符串、空 DataFrame、空 list 每种都保护 |
| 1.4 | 类型守卫 | `isinstance` 检查或类型注解 + 入口校验 |
| 1.5 | 平行入口语义一致 | 同一功能的多个入口（CLI / Flask API / 直接调用）产出结果须一致 |
| 1.6 | 配置声明 vs 使用 | 声明了的配置项（如 `MAX_CONCURRENT_TASKS`）须实际被引用和执行 |
| 1.7 | 多阶段时序依赖 | 先准备后执行的架构中，准备阶段不能依赖执行阶段的产出 |

### 类别 2：错误处理机制（error）

| # | 检查项 | 说明 |
|---|--------|------|
| 2.1 | except 不吞异常 | `except:` 或 `except Exception:` 至少 log 错误信息 |
| 2.2 | fallback 不降级数据质量 | 无正确数据时 raise，不 fillna(0) 静默替换 |
| 2.3 | 除法防零 | 除数可能为零时显式检查 |
| 2.4 | DB 连接失效保护 | 连接获取后检查有效性，操作后正确关闭/归还 |
| 2.5 | 文件不存在 ≠ 数据为空 | `FileNotFoundError` 和 `len(df)==0` 分别处理 |
| 2.6 | 错误消息含上下文 | 打印文件路径、日期、表名、行数 |
| 2.7 | 错误分级合理 | 可恢复用 warning/log，不可恢复用 raise |
| 2.8 | finally 清理资源 | DB cursor、文件句柄、临时文件须在 finally 中关闭 |

### 类别 3：数据格式与完整性（data）

| # | 检查项 | 说明 |
|---|--------|------|
| 3.1 | 列名统一 | 读入后显式 rename 到标准列名 |
| 3.2 | 数据类型强制转换 | `pd.to_numeric(errors='coerce')` 后不能 `fillna(0)` 掩盖错误 |
| 3.3 | NaN/None 处理策略 | 关键数值列 NaN 应 raise error，非关键列可有明确的填充策略 |
| 3.4 | 日期格式一致 | 统一 `yyyy-MM-dd` 或 `yyyyMMdd`，不混用 |
| 3.5 | DataFrame 合并对齐 | `pd.merge` / `concat` 前检查 key 唯一性，merge 后检查行数膨胀 |
| 3.6 | 重复行检测 | 写入前按业务主键去重或断言无重复 |
| 3.7 | 输入验证 vs 静默修复 | 脏数据应被拒绝（hard fail）或隔离（quarantine），不能被静默"修复"成合法值 |

### 类别 4：SQL 与持久化（sql）— 仅含 DB 操作的文件

| # | 检查项 | 说明 |
|---|--------|------|
| 4.1 | SQL 注入防护 | 使用参数化查询 `%s` + params，禁止 f-string 拼值 |
| 4.2 | DELETE 范围匹配业务主键 | `DELETE WHERE date=X` 不够 → 须加 `AND score_name=Y` / `AND index_code=Z` 等业务维度 |
| 4.3 | 写入幂等 | DELETE + INSERT 或 `INSERT ... ON DUPLICATE KEY UPDATE` |
| 4.4 | 事务完整性 | 批量操作须在事务内，失败时 rollback |
| 4.5 | 连接池管理 | 连接用完归还，不泄漏；长事务不锁表 |
| 4.6 | 批量写入效率 | 大量 INSERT 用 `executemany` 或 `LOAD DATA`，不逐行 INSERT |

### 类别 5：并发与进程管理（concurrency）— 仅含线程/进程操作的文件

| # | 检查项 | 说明 |
|---|--------|------|
| 5.1 | 并发限流实施 | 声明了 MAX_CONCURRENT_TASKS 就须用 Semaphore/Queue 执行限流 |
| 5.2 | 线程安全的共享状态 | 多线程写同一 dict/list 须加锁（`threading.Lock`） |
| 5.3 | 进程生命周期管理 | subprocess/MATLAB engine 须有超时、异常终止、资源回收 |
| 5.4 | daemon 线程的风险 | `daemon=True` 的线程在主进程退出时被强杀，须确认不会丢失关键数据 |
| 5.5 | 背压机制 | 高并发下须有排队或拒绝（HTTP 429），不能无限起线程/进程 |
| 5.6 | 任务状态存储线程安全 | `_task_store` 的读写须在 `_task_lock` 内 |

### 类别 6：API 与路由（api）— 仅含 Flask 路由的文件

| # | 检查项 | 说明 |
|---|--------|------|
| 6.1 | 参数校验 | 必填参数缺失时返回 400 + 明确错误信息 |
| 6.2 | 错误响应格式 | 统一 JSON 格式 `{"error": "...", "status": "failed"}` |
| 6.3 | 异步任务状态 | 提交后返回 task_id，可通过 GET 查询状态 |
| 6.4 | 请求体大小限制 | 大文件上传须限制大小 |
| 6.5 | CORS 配置 | 生产环境不能 `CORS(app, origins='*')` |
| 6.6 | 路由参数与 MATLAB 入口一致 | Flask 传给 MATLAB 的参数须与 MATLAB 函数签名匹配 |
| 6.7 | 路由与 MATLAB 结果语义一致 | Flask 路由调用的 MATLAB 入口（如 run_optimizer）须与其他入口（如 run_optimizer_json）产出语义等价的结果 |

### 类别 7：测试验证（test）

| # | 检查项 | 说明 |
|---|--------|------|
| 7.1 | 测试断言与实现同步 | 断言的值/条件须与当前代码行为一致，不能是旧版残留 |
| 7.2 | 关键路径有覆盖 | 数据写入、删除、并发控制等关键逻辑有对应测试 |
| 7.3 | 边界值测试 | 空输入、单条、超大批量 |
| 7.4 | 幂等性测试 | 同一操作跑两次结果一致 |
| 7.5 | mock 粒度合理 | 不过度 mock（如 mock 掉了被测逻辑本身） |

## 第四步：严重度分级

| 级别 | 含义 | 例子 |
|------|------|------|
| P0 | 数据错误/静默丢数据/安全漏洞 | DELETE 范围过大误删数据、fillna(0) 污染、SQL 注入 |
| P1 | 可能出错但未必触发 | 无并发限流、daemon 线程丢数据、类型未检查 |
| P2 | 代码规范/可维护性 | 命名不一致、缺类型注解、配置硬编码 |

## 第五步：输出

按 P0→P1→P2 排序输出检查结果，格式如下：

```
## /review-py <file.py> (depth=N)

### <file.py>（层 1）
| # | 检查项 | 严重度 | 结果 | 说明 |
|---|--------|--------|------|------|
| 4.2 | DELETE 范围匹配主键 | P0 | ❌ | line 403 只按 date 删除，缺 score_name 维度 |
| 3.2 | 数据类型强制转换 | P0 | ❌ | line 356 fillna(0) 掩盖脏数据 |
| ... | ... | ... | ... | ... |

### <callee.py>（层 2）
| ... | ... | ... | ... | ... |

### 调用链
目标文件 → callee1.py → callee2.py
         → callee3.py

### 汇总
✅ 通过: N  ❌ P0: N  ⚠️ P1: N  💡 P2: N  N/A: N
```

**如果使用了 `--fix`，将 P0+P1 的失败项传入 `/fix` 流程处理。**
