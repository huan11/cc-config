审计并精简项目的全部知识文件，在零知识丢失的前提下去重、抽象、精简。

参数：`/distill [--scope all|rules|commands|todo] [--fix] [--verbose]`

| 参数 | 默认 | 说明 |
|------|------|------|
| `--scope` | `all` | `all`=全部 / `rules`=仅CLAUDE.md / `commands`=仅斜杠命令 / `todo`=仅TODO+best-practices |
| `--fix` | 关 | 分析后自动执行修改（默认仅报告） |
| `--verbose` | 关 | 显示每条变更的 before/after diff |

## 知识层级

```
L0  ~/.claude/CLAUDE.md          全局铁律（跨项目通用）
L1  <project>/CLAUDE.md          项目规则（本项目特有）
L2  .claude/commands/*.md        领域检查清单（review/fix/audit等）
L3  TODO.md / best-practices.md  时态知识（待办/经验积累）
L4  memory/MEMORY.md             会话记忆（跨对话持久）
```

**层级规则：**
- 规则向上冒泡：具体经验 → 验证后提升为通用规则
- 重复向下清理：高层已有的规则，低层不再重复（可引用）
- 同层不重复：同一规则只在一个文件中完整定义

## 目标文件

按 scope 确定扫描范围：

| scope | 文件 |
|-------|------|
| rules | `~/.claude/CLAUDE.md` + `<project>/CLAUDE.md` |
| commands | `.claude/commands/*.md` 全部 |
| todo | `TODO.md` + `docs/best-practices.md` |
| all | 以上全部 + `memory/MEMORY.md` |

## Phase 1: 盘点

逐文件读取，提取**知识单元(KU)**：

| KU 类型 | 识别方式 |
|---------|---------|
| 规则 | 祈使句：必须/禁止/须/不能/always/never |
| 检查项 | 表格行（`\| # \| 检查项 \|`） |
| TODO | `- [ ]` 或 `- [x]` |
| 模式/陷阱 | 经验性描述：典型场景/常见问题/踩坑 |
| 配置事实 | 环境/路径/格式/版本等声明 |

每条 KU 标记：`[文件:行号] [类别] [层级L0-L4]`

**输出：** 知识清单表

```
| 文件 | 行数 | KU数 | 类型分布 |
```

## Phase 2: 去重

对全部 KU 按语义分组（同一件事的不同表述归为一组）。

**判定标准：**
- 两条 KU 描述的行为约束相同（虽措辞不同）→ 重复
- 一条 KU 是另一条的子集 → 子集标记为冗余
- 两条 KU 互为补充且上下文相同 → 合并

**每组重复的处置：**
1. 选定 canonical 位置 = 最高适用层级的文件
2. 其余位置标记为"删除"或"改为引用"

**输出：** 重复规则表

```
| 规则语义 | 出现位置 | canonical | 处置 |
| 除法防零 | CLAUDE.md:27, review.md:2.3, fix.md:4行 | CLAUDE.md | review/fix删除，已被全局覆盖 |
```

## Phase 3: 抽象

找到多个具体 KU 可合并为更高层原则的情况。

**判定标准：**
- N≥2 个 KU 分布在不同文件，描述的是同一原则的不同实例
- 抽象后的原则比任何一个实例都更通用
- 抽象规则应放在更高层级（L2→L1，L1→L0）

**交叉验证方法：**
- MATLAB review 和 Python review-py 中相同的检查项 → 可能是语言无关原则
- 多个 command 中重复出现的流程步骤 → 可能是通用工作流原则
- fix.md 和 review.md 共享的检查逻辑 → 可能是更高层的代码质量原则

**输出：** 抽象提案表

```
| 具体规则A | 具体规则B | → 抽象规则 | 建议层级 |
| review 1.8 同逻辑多副本 | review-py 1.8 同逻辑多副本 | "同逻辑N副本修一处须全部同步" | L1 项目CLAUDE |
```

## Phase 4: 鲜度

检查时态知识的新鲜度：

| 检查 | 方法 | 处置 |
|------|------|------|
| TODO 已完成 | grep 代码验证修复是否存在 | 移入"已完成"折叠区 |
| TODO 过期 | 超过 30 天未动且无阻塞说明 | 标记 stale，问用户 |
| best-practice 已融入 command | 比对 command 检查项 | 从 best-practices 删除 |
| MEMORY 与 CLAUDE.md 重复 | 比对内容 | 从 MEMORY 删除 |
| MEMORY 事实已变 | 对比代码现状 | 更新或删除 |

**输出：** 鲜度检查表

```
| 条目 | 状态 | 处置 |
| TODO H4 partial语义 | 仍开放 | 保留 |
| MEMORY "255s→122s" | 可能过时 | 验证 |
```

## Phase 5: 精简

逐文件评估可压缩的冗余表述：

| 精简手法 | 例子 |
|---------|------|
| 多段说明→内联一句 | "检查方法：当函数...搜索所有下游..." → 表格单元格一句话 |
| 三列表→两列表 | `# \| 检查项 \| 说明` → `# \| 检查项(含说明)` |
| 重复模板→引用 | review-py 输出格式 → "同 /review 格式" |
| 相近检查项合并 | 7.1正常+7.2边界+7.3类型 → 7.1正常/边界/类型覆盖 |

**约束：** 精简后语义必须等价，不能丢失判断所需的关键信息。

**输出：** 精简统计

```
| 文件 | 原行数 | 精简后 | 方法 |
```

## Phase 6: 报告

```
## /distill 分析报告

### 知识资产概览
| 层级 | 文件数 | 行数 | KU数 |
| L0 全局 | 1 | 38 | 12 |
| L1 项目 | 1 | 35 | 10 |
| L2 命令 | 27 | 2760 | ~300 |
| L3 时态 | 2 | 100 | 20 |
| L4 记忆 | 1 | 30 | 15 |

### 发现

#### 1. 重复 (N处)
| 规则 | 出现位置 | canonical | 处置 |

#### 2. 可抽象 (N处)
| 具体A + 具体B → 抽象规则 | 建议层级 |

#### 3. 过时 (N处)
| 条目 | 状态 | 处置 |

#### 4. 错位 (N处)
| 规则 | 当前位置 | 建议位置 | 原因 |

#### 5. 可精简 (N处)
| 文件 | 原行数 | 精简后 | 缩减 |

### 汇总
知识单元: N → 去重后 M (重复N, 抽象N, 过时N)
总行数: N → M (-X%)

### 操作计划
| 序号 | 操作 | 文件 | 描述 |
| 1 | 迁移 | review.md→CLAUDE.md | "同逻辑N副本同步"提升为项目规则 |
| 2 | 删除 | fix.md:45 | "除法防零"已在CLAUDE.md |
| 3 | 精简 | hooks-guide.md | 529→200行，内联说明 |
| 4 | 归档 | TODO.md | 3条已完成→折叠 |
```

**`--fix` 时：** 先 `git stash push -m "distill-backup"`，然后逐条执行操作计划。每条执行后验证文件无语法错误。

## 保全原则

1. **删除前必须确认替代** — 被删的 KU 在 canonical 位置有等价覆盖
2. **精简前后 diff** — 确认语义不变，只是表述更紧凑
3. **抽象保留实例** — 抽象规则到高层后，低层保留具体实例（如 MATLAB 特有陷阱）
4. **操作可逆** — git stash 快照，每步可回滚
5. **不动正确的冗余** — 如果重复是刻意的（如 fix.md 速查表复制 review 的子集作为快速参考），标注但不删除
