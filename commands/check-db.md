---
description: 检查数据库 Schema、表结构和注释
disable-model-invocation: true
---

检查项目数据库的 Schema 结构和注释完整性，一次性覆盖"结构是否正确"和"注释是否完善"两大维度。

## 检查流程

### 1. 获取连接信息

- 读取 `database/.env`、`database/docker-compose.yml`、项目配置文件，提取数据库连接信息（禁止输出密码）
- 如果无法确定连接信息，询问用户

### 2. 采集数据库元信息

- `SHOW DATABASES` 列出所有 schema（排除系统库）
- 对每个业务 schema 执行 `SHOW TABLES`
- 对每张表执行 `SHOW CREATE TABLE`，记录完整建表语句
- 查询 `information_schema.TABLES` 获取 `TABLE_COMMENT`
- 查询 `information_schema.COLUMNS` 获取字段名、类型、是否可空、默认值、`COLUMN_COMMENT`

### 3. 扫描代码中的建库建表行为

在项目源码中搜索：
- SQL 语句：`CREATE DATABASE`、`CREATE TABLE`、`CREATE INDEX`、`ALTER TABLE`
- ORM 模型定义：SQLAlchemy `Base.metadata.create_all`、Django `makemigrations/migrate`
- Migration 文件：Alembic `versions/`、Django `migrations/`
- 代码中直接拼接的 DDL 字符串
- `IF NOT EXISTS` 模式的动态建表

### 4. Schema 结构对比

将代码中定义的表与数据库实际表进行对比，标记：仅在代码中（未建表）、仅在数据库中（无对应代码）、两者都有。

### 5. 注释完整性检查

逐表逐字段检查：

**缺失类：**
- 表无注释（`TABLE_COMMENT` 为空）
- 字段无注释（`COLUMN_COMMENT` 为空）
- 注释是占位符（`TODO`、`-`、`无`、`暂无`、`略`）

**可读性类：**
- 纯英文缩写无解释（`amt`、`qty`、`flg`、`tp`）
- 含义模糊（`类型`、`状态`、`标记`，无枚举值说明）
- 状态/类型字段缺少枚举说明
- 注释与字段名重复无信息增量

### 6. 代码与数据库注释对比（如有 ORM）

- 扫描 ORM 模型中的 `comment=`、`help_text`、`verbose_name`
- 对比代码注释与数据库实际注释是否一致

## 输出格式

### 一、Schema 总览

| Schema | 表数量 | 来源 | 无注释表 | 字段总数 | 无注释字段 |
|--------|--------|------|---------|---------|-----------|

### 二、结构对比

| 表名 | 数据库中 | 代码中 | 状态 |
|------|---------|--------|------|
| users | ✅ | ✅ | 一致 |
| logs | ✅ | ❌ | 仅数据库 |
| orders | ❌ | ✅ | 未建表 |

### 三、注释问题清单

**表级：**

| Schema | 表名 | 当前注释 | 问题 | 建议注释 |
|--------|------|---------|------|---------|

**字段级（按表分组）：**

| 字段 | 类型 | 当前注释 | 问题 | 建议注释 |
|------|------|---------|------|---------|

### 四、代码与数据库不一致（如有 ORM）

| 表名 | 字段 | 数据库注释 | 代码注释 | 建议 |
|------|------|-----------|---------|------|

### 五、SQL 脚本建议

如果发现代码中有建表行为，给出是否需要抽取为独立 SQL 脚本的建议。

### 六、总结

- P0（结构不一致 / 缺少注释）：X 项
- P1（注释可读性差）：Y 项
- P2（代码与数据库不一致）：Z 项
- 需用户确认：N 项

## 守护栏

- 禁止在输出中粘贴数据库密码
- 禁止执行任何写操作，只读检查
- 无法连接数据库时，仅基于代码分析，明确标注"未连接数据库"
- 无法从字段名推断含义的，标记为"需询问用户"，禁止编造
- 建议注释仅供参考，最终措辞由用户确认
