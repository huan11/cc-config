---
description: 一键执行 make-* 命令族全套分析（repo-card → repo-behaviors → exec-flow → algo-flow），对目标仓库做完整取证。批量处理项目时使用。
---

对指定仓库依次执行全部 4 个 make-* 分析步骤，一次性生成完整的仓库文档集。

目标仓库路径: $ARGUMENTS

如果未提供路径，立即询问用户，不要猜测。

## 产出物

在目标仓库的 `docs/` 下生成：
1. `repo-card.md` — 仓库身份卡片
2. `repo-behaviors.md` — 核心行为 + 业务规则
3. `exec-flow.md` — 程序执行流图
4. `algo-flow-[名称].md` — 核心算法流程图（可能多个）

## 执行流程

按以下顺序执行，每一步的产出是下一步的输入上下文：

### Step 1: Repo Card

执行 `/make-repo-card` 的完整流程，生成 `docs/repo-card.md`。

完成后向用户展示摘要，但**不等确认，直接进入下一步**（和单独执行时的区别）。

### Step 2: Repo Behaviors

基于 Step 1 产出的 repo-card，执行 `/make-repo-behaviors` 的完整流程，生成 `docs/repo-behaviors.md`。

完成后展示摘要，直接进入下一步。

### Step 3: Exec Flow

基于 Step 1 的入口信息，执行 `/make-exec-flow` 的完整流程，生成 `docs/exec-flow.md`。

完成后展示 ASCII 执行流图，直接进入下一步。

### Step 4: Algo Flow

基于 Step 3 的执行流图，**自动选择核心层中最复杂的 1-2 个函数**，执行 `/make-algo-flow` 的完整流程。

选择标准：优先选 exec-flow 中标注为"核心层"的函数，其次选包含最多业务规则（来自 Step 2）的函数。

### Step 5: 汇总展示

全部完成后，向用户展示总览：

```
## 取证完成: [仓库名]

### 产出文件
- docs/repo-card.md
- docs/repo-behaviors.md
- docs/exec-flow.md
- docs/algo-flow-[名称].md

### 摘要
- 项目定位: [一句话]
- 技术栈: [语言 + 框架]
- 核心行为: N 条
- 业务规则: M 条
- 执行层级: K 层
- 算法流程图: P 个（[函数名列表]）

### 待确认项汇总
<!-- 从所有文档中汇集 -->
```

等待用户确认或修改意见。

## 守护栏

- **中间步骤不阻塞** — 和单独执行不同，中间步骤展示摘要后直接推进，不等用户确认。只在最后汇总时等待。这是批量处理的核心体验。
- **步骤间传递上下文** — 每一步都要读取前面步骤的产出，不要重新采集已有信息。这是串联的核心价值，避免重复劳动。
- **algo-flow 自动选择** — 不询问用户选哪个函数，自动选最有价值的 1-2 个。用户可以在最终确认时要求补充。
- **任一步骤失败不中断** — 如果某一步无法产出（比如没有 SQL 依赖导致数据库相关内容为空），跳过并在汇总中标注，继续下一步。
