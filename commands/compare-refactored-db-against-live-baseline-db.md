---
description: 对比两个数据库环境的数据一致性，支持同机器或跨机器对比。
argument-hint: baseline:<host>:<port>:<user>:<password>:<db> target:<host>:<port>:<user>:<password>:<db> [--date YYYY-MM-DD] [--tables t1,t2]
---

对比两个数据库环境的数据，A（baseline）为基准，B（target）为待验证。支持同一台机器的两个库，也支持跨机器对比（如生产只读账号 vs 开发机）。

## 参数

由用户提供，缺少时必须询问。**不从项目配置文件读取任何连接信息。**

### 连接信息

两组连接，格式为 `baseline:<host>:<port>:<user>:<password>:<db>` 和 `target:<host>:<port>:<user>:<password>:<db>`。

- `baseline`：基准库（通常是生产库的只读账号）
- `target`：待验证库（通常是重构库）

### 参数示例

同机器两个库：
```
/compare-refactored-db-against-live-baseline-db baseline:192.168.1.100:3306:readonly:Pass1:data_prepared_new target:192.168.1.100:3306:wc:Pass2:data_prepared_new_dev --date 2026-03-28
```

跨机器对比（生产只读 vs 开发机）：
```
/compare-refactored-db-against-live-baseline-db baseline:8.141.115.103:3306:readonly:Pass1:data_prepared_new target:192.168.1.200:3306:wc:Pass2:data_prepared_new_dev
```

### 对比范围

- **--tables**：表名列表（逗号分隔，或 `*` 表示全部）。不指定则从 `docs/db-inventory-*.md` 读取表清单作为对比范围。
- **--date**：单个日期（YYYY-MM-DD）或日期范围（`2026-03-01,2026-03-28`）。不指定则对比最近 3 个交易日。

禁止在输出中粘贴密码。如果密码中包含冒号，用户应将密码用引号包裹。

## 前置条件

`docs/db-inventory-*.md` 必须存在。如果不存在，提示用户先运行 `/extract-db-inventory-from-repo-outputs`。

db-inventory 提供：
- **表清单**：作为默认对比范围（当 `--tables` 未指定时）
- **表注释**：丰富报告（如 `data_index — 指数日行情数据`）
- **逻辑键**：当 information_schema 无主键时作为 fallback（标注"来自 db-inventory，非 DB 约束"）

## 执行方式

用 Python 脚本执行（`uv run`），通过 pymysql 分别连接两端，数据拉到 pandas DataFrame 后在内存中对比。**禁止跨库 JOIN**（除非检测到 host:port 相同，可选用 JOIN 优化）。

### 主键检测

优先级：
1. 用户提供主键信息时优先使用
2. 查询 `information_schema.KEY_COLUMN_USAGE` 获取 PRIMARY KEY 列（数据库实际约束，最可靠）
3. 复合主键：用所有主键列组合作为对比键
4. DB 无主键时，fallback 到 `docs/db-inventory-*.md` 中的逻辑键（来自 YAML `private_keys`，是应用层 delete 条件，不一定是真主键，报告中标注"逻辑键，非 DB 约束"）
5. 以上都没有：跳过维度 4/5/6，仅对比列名、类型、行数，标注"无主键，跳过行级对比"

### 日期列检测

按以下优先级确定每张表的日期列：
1. `valuation_date` 列（项目约定的标准日期字段）
2. db-inventory 逻辑键中类型为日期的字段
3. `information_schema.COLUMNS` 中类型为 `date` / `datetime` 的列（若唯一则自动选用，多个则询问用户）
4. 以上都没有 → 标记为"无日期列"，整表一次性对比

### 日期遍历策略

当指定了日期范围但未指定具体单日时：
1. 用上述优先级确定日期列
2. 以 B 库为驱动，查询该日期列的所有不重复值
3. 逐日期从 A/B 分别拉取数据对比
4. 无日期列的表整表一次性对比

### 大表保护

- 单表（过滤后）超过 **10 万行** 时，提示用户确认是否继续
- 超过 **100 万行** 时，自动切换为采样模式：随机取 10 万行对比，结论标注"采样对比"

## 对比维度

逐表按以下 7 项检查：

| # | 维度 | 检查内容 |
|---|------|---------|
| 1 | 列名 | 是否完全一致，顺序是否一致，B 有无缺少或多余列 |
| 2 | 数据类型 | 同名列的存储类型是否一致（查 information_schema.COLUMNS） |
| 3 | 行数 | 同条件下两边 `COUNT(*)` 是否相等 |
| 4 | 主键覆盖 | A 有但 B 没有的行、B 有但 A 没有的行，各最多 5 条样本 |
| 5 | 数值一致性 | 按主键 merge，对数值列计算最大绝对差和最大相对差，容差 1e-6 |
| 6 | NULL 一致性 | 同主键同列，一边 NULL 另一边有值，列出数量和最多 5 条样本 |
| 7 | 日期覆盖 | （仅日期遍历模式）B 有但 A 没有的日期、A 有但 B 没有的日期 |

## 输出

### 输出文件

写入 `docs/db-compare-report-{YYYYMMDD}.md`。

### 表级结构检查（每表输出一次）

**表: `data_index` — 指数日行情数据** *(表注释来自 db-inventory)*

| 维度 | 结论 | 详情 |
|------|------|------|
| 列名 | ✅ | 一致 |
| 数据类型 | ⚠️ | `amount`: A=decimal(18,4) B=decimal(10,2) |

### 逐日期明细（日期遍历模式）

> 仅输出有差异的日期，无差异的汇总为一行："共 N 天数据完全一致"。

| 日期 | 行数 A/B | 主键覆盖 | 数值一致性 | NULL 一致性 | 结论 |
|------|---------|---------|-----------|------------|------|
| 2025-01-03 | 3500/3498 | ❌ A多2行 | ⚠️ max_diff=0.001 | ✅ | ❌ |
| 其余 98 天 | - | - | - | - | ✅ 全部一致 |

### 汇总

| 表名 | 表注释 | 日期范围 | 总天数 | 一致天数 | 差异天数 | 结论 | 差异摘要 |
|------|--------|---------|--------|---------|---------|------|---------|

> 每张表输出完立即给出结论，不要等所有表跑完再输出。

### 汇总建议

| 差异类别 | 数量 | 严重度 | 建议 |
|---------|------|--------|------|
| Schema 不一致 | 2 张表 | 高 | 检查重构代码的建表逻辑 |
| 行数不一致 | 3 张表 | 中 | 检查数据写入的过滤条件 |
| 值微小偏差 (< 0.01) | 1 张表 | 低 | 可能是浮点精度差异，可忽略 |
| 值显著偏差 (> 1.0) | 0 | — | — |

## 守护栏

- 禁止执行任何写操作（INSERT / UPDATE / DELETE / DROP）
- 只读对比，不修改任何数据库
- 禁止在输出中粘贴数据库密码
- 无法连接某一端时，明确报错并停止，禁止猜测数据
- 对比结果有疑问时，如实标注，不编造解释
- 禁止猜测主键或日期列名，检测失败时必须询问用户
- 充分利用数据库聚合函数（COUNT / SUM / CHECKSUM）预判差异，减少不必要的全量拉取
- **跨机器兼容** — 默认通过 Python 拉取两边数据在内存对比；仅当 host:port 相同时才用跨库 JOIN 优化
- **生产库保护** — SQL 必须带 WHERE 条件（至少限定日期），禁止全表扫描
- **不从项目配置文件读取连接信息** — 所有凭据由用户提供
