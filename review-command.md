---
description: 审查斜杠命令/Skill 的设计质量，按 6 大类 25+ 检查项逐项检查，输出问题清单和改进建议
argument-hint: <命令文件路径> [--fix] [--batch <glob>]
disable-model-invocation: true
---

审查 Claude Code 斜杠命令（.md 文件）的设计质量，基于官方最佳实践和实战经验。

参数：`/review-command <path> [--fix] [--batch <glob>]`

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `<path>` | 必填 | 目标命令文件路径（支持 .md） |
| `--fix` | 关闭 | 审查完自动修复 P0+P1 问题 |
| `--batch <glob>` | 关闭 | 批量审查，如 `--batch .claude/commands/*.md` |

## 第一步：读取目标文件

读取目标 .md 文件全文。如果是 `--batch` 模式，列出匹配的文件清单，逐个审查。

识别文件类型：

| 类型 | 识别方式 |
|------|---------|
| commands 格式 | 路径含 `/commands/` 且为单 .md 文件 |
| skills 格式 | 路径含 `/skills/` 且文件名为 `SKILL.md` |

如果文件不存在或不是 .md 文件，提示用户检查路径。

## 第二步：解析 Frontmatter

提取 YAML frontmatter 中的所有字段。如果没有 frontmatter（无 `---` 包裹的 YAML 块），标记为 P1 问题。

记录以下字段的存在性和值：
- name, description, argument-hint
- disable-model-invocation, user-invocable
- allowed-tools, model, context, agent

## 第三步：逐类检查

### 类别 1：元信息（meta）

| # | 检查项 | 严重度 | 判定标准 |
|---|--------|--------|---------|
| 1.1 | description 存在 | P0 | 缺少 description 导致 Claude 不知道何时使用该命令 |
| 1.2 | description 用第三人称 | P1 | 不能用"我可以..."或"你可以..."，应描述命令做什么 |
| 1.3 | description 含触发场景 | P1 | 应同时说明"做什么"和"何时用"，而非只说做什么 |
| 1.4 | description 长度合理 | P2 | 不超过 1024 字符；太短（<10字）可能不够精确 |
| 1.5 | name 格式合规 | P1 | 仅限小写字母+数字+连字符，不超过 64 字符，不含保留词（anthropic、claude） |
| 1.6 | name 语义清晰 | P2 | 不能是 helper/utils/tools 等模糊词，应直接表达用途 |
| 1.7 | argument-hint 存在（如命令接受参数） | P2 | 命令正文含 `$ARGUMENTS` 但 frontmatter 无 argument-hint |

### 类别 2：触发控制（invocation）

| # | 检查项 | 严重度 | 判定标准 |
|---|--------|--------|---------|
| 2.1 | 有副作用的命令设置了 disable-model-invocation | P0 | 正文含写文件、git commit、部署、发消息等操作，但未设置 `disable-model-invocation: true` |
| 2.2 | allowed-tools 最小化 | P2 | 只读分析类命令不应授权 Write/Edit 等写工具 |
| 2.3 | context: fork 用于隔离型任务 | P2 | 重度研究/调研类命令建议用 `context: fork` 避免撑爆主对话上下文 |

### 类别 3：结构与工作流（structure）

| # | 检查项 | 严重度 | 判定标准 |
|---|--------|--------|---------|
| 3.1 | 有分阶段工作流 | P1 | 非平凡命令（>30行）应有 Phase/Step 分阶段结构，而非一段散文 |
| 3.2 | 不可逆操作前有确认门 | P0 | 涉及删除、部署、提交、发消息等操作但没有"等待用户确认"步骤 |
| 3.3 | 有输出格式模板 | P1 | 缺少结构化输出模板，每次运行结果格式可能不一致 |
| 3.4 | 参数有文档 | P1 | 命令使用 `$ARGUMENTS` 但正文中没有参数说明表格 |
| 3.5 | 缺失参数有处理 | P2 | 参数缺失时是否有明确行为（询问用户 / 使用默认值 / 报错） |
| 3.6 | 主文件行数 <= 500 | P2 | 超过 500 行应拆分到支撑文件（渐进式披露） |
| 3.7 | 引用链不超过一层 | P2 | SKILL.md → 支撑文件 → 再引用其他文件（过深，Claude 可能只部分读取） |

### 类别 4：内容质量（content）

| # | 检查项 | 严重度 | 判定标准 |
|---|--------|--------|---------|
| 4.1 | 不解释 Claude 已知的常识 | P2 | 解释"什么是 PDF"、"Python try/except 语法"等 Claude 本来就知道的知识 |
| 4.2 | 自由度匹配脆弱性 | P1 | 脆弱操作（数据库迁移）给了过高自由度，或简单操作（代码审查）限制过死 |
| 4.3 | 术语一致 | P2 | 同一概念在文中用了多个不同词汇指代 |
| 4.4 | 无时效性信息 | P2 | 含"在 X 月之前用旧方式"等会过时的表述 |
| 4.5 | 文件路径用正斜杠 | P1 | 使用了 `scripts\helper.py` 等 Windows 风格路径 |
| 4.6 | 默认方案而非选择题 | P2 | 列出多个等价方案让 Claude 选择，而非给一个默认+特殊逃生口 |

### 类别 5：守护栏（guardrails）

| # | 检查项 | 严重度 | 判定标准 |
|---|--------|--------|---------|
| 5.1 | 有守护栏 section | P0 | 完全没有"守护栏"/"Guardrails"/"注意事项"等约束性 section |
| 5.2 | 禁止编造 | P1 | 守护栏未明确禁止编造不存在的内容（URL、数据、代码逻辑） |
| 5.3 | 不确定时的行为 | P1 | 未说明信息不足时应询问用户而非猜测 |

### 类别 6：生态关系（ecosystem）

| # | 检查项 | 严重度 | 判定标准 |
|---|--------|--------|---------|
| 6.1 | 声明与相关命令的关系 | P2 | 属于命令家族但未说明与其他命令的关系和使用顺序 |
| 6.2 | 无功能重叠 | P2 | 与同目录下其他命令功能高度重叠（需读取同目录其他命令判断） |

## 第四步：严重度分级

| 级别 | 含义 | 例子 |
|------|------|------|
| P0 | 严重设计缺陷，可能导致误触发或数据损失 | 有副作用但无 disable-model-invocation、无确认门、无守护栏 |
| P1 | 影响可用性或可靠性 | 无 description、无输出模板、无参数文档、自由度不匹配 |
| P2 | 可优化项 | 术语不一致、name 不够清晰、行数偏长 |

## 第五步：输出

按 P0 → P1 → P2 排序，格式如下：

```
## /review-command <文件路径>

### 基本信息

| 项目 | 值 |
|------|------|
| 文件 | <路径> |
| 格式 | commands / skills |
| 行数 | N |
| 有 frontmatter | 是/否 |
| name | <值或缺失> |
| description | <前30字...或缺失> |

### 检查结果

| # | 检查项 | 严重度 | 结果 | 说明 |
|---|--------|--------|------|------|
| 1.1 | description 存在 | P0 | pass/fail | ... |
| 2.1 | 副作用命令禁自动触发 | P0 | pass/fail/N/A | ... |
| ... | ... | ... | ... | ... |

### 汇总

pass: N  P0: N  P1: N  P2: N  N/A: N

### 评级

- **A（优秀）** — 无 P0，P1 <= 1，可直接使用
- **B（良好）** — 无 P0，P1 <= 3，小幅调整即可
- **C（需修改）** — 有 P0 或 P1 >= 4，需修改后使用
- **D（需重写）** — P0 >= 3，存在根本性设计问题

### 改进建议

按优先级列出具体修改建议，每条给出修改前后的对比：

**P0-1: [问题描述]**
- 当前: <原文摘要>
- 建议: <改写后的内容>
```

**`--batch` 模式额外输出汇总表：**

```
## 批量审查汇总

| 文件 | 行数 | 评级 | P0 | P1 | P2 |
|------|------|------|----|----|-----|
| commit.md | 85 | A | 0 | 1 | 2 |
| deploy.md | 120 | C | 2 | 3 | 1 |
| ... | ... | ... | ... | ... | ... |

最差: deploy.md (C), 建议优先修复
```

**`--fix` 模式：** 审查完成后，自动对 P0 和 P1 问题生成修复，逐项向用户展示修改 diff，确认后写入。

## 智能判断规则

以下规则帮助判断非黑即白的检查项：

### 判断"有副作用"（2.1）
正文含以下任一关键词/模式即判定为有副作用：
- git commit / git push / git add
- 写入文件、创建文件（Write、保存到、写入、创建）
- 部署、发布（deploy、publish）
- 发送消息（send、post、comment）
- 删除（delete、rm、remove、DROP）
- API 调用（curl、fetch、POST、PUT、DELETE 请求）

### 判断"非平凡命令"（3.1）
主体正文超过 30 行、或含多个步骤/阶段描述的命令。纯参考型内容（conventions、style guide）除外。

### 判断"命令家族"（6.1）
以下信号表明命令属于一个家族：
- 同目录下有多个命令（如 `gzh/` 目录下的 scan、topic、write）
- 命令名有共同前缀（如 opsx:new、opsx:apply）
- 正文中提到其他命令名

## 守护栏

- 不替用户改写命令内容，只在"改进建议"中给出方向
- `--fix` 模式下每项修改必须展示 diff 并经用户确认
- 检查基于已有内容，不因"没写某个 section"就判定为错误——有些简短命令天然不需要复杂结构
- 对姿态型命令（如 explore）放宽流程结构要求，它们可以不分阶段
- N/A 判定要合理：纯参考型命令（user-invocable: false）不需要确认门和输出模板
- 不编造问题——pass 就是 pass，不找茬
