---
description: 提取待办追加到 TODO.md
disable-model-invocation: true
---

回顾当前对话完整历史，找出：
1. 提到但**没做完**的任务
2. 发现但**没修复**的问题
3. 讨论过的**优化建议**但没落地的

整理成列表展示给用户确认，格式：

```
提取到以下待办：
1. [高] xxx
2. [中] yyy
```

用户确认后，追加写入项目根目录 `TODO.md`，格式：

```markdown
## YYYY-MM-DD

- [ ] xxx
- [ ] yyy
```

规则：
- 只追加，不覆盖已有内容
- 同一天追加到同一个日期标题下
- 不要编造，只提取对话中实际出现的内容
- 不确定的先问用户
