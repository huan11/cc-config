## 探索边界（硬规则，优先于一切效率考虑）

1. **只在用户点明的范围里探索。** 用户给了哪些仓库 / 目录 / 文件，范围就是它们本身——**不含父目录，不含兄弟目录**。
   - `grep` / `find` / `ls` / `rg` 的根必须落在那些路径上。不许为了"一次扫全"退到父目录再筛。
   - 界外发现了更相关的材料，**不构成**自行扩大范围的理由。先停下报一句「界外有 X，要不要纳入」，拿到同意再读。
   - 需要判断本地有没有现成克隆这类前置探查，用精确路径匹配，探到即停，不顺手扫内容。

2. **当前工作目录只有一个：Claude Code 启动时所在的那个目录**（每次会话由 system prompt 的 Environment 段落给出 `Primary working directory`；不确定就跑一次 `pwd` 确认，Bash 每次调用开始时都在它里面）。Bash 工具里的 `cd` **不跨调用保持**，每次调用结束会被重置回该目录。因此：
   - 一律用**绝对路径**，不要靠 `cd` 建立"我现在在哪"的错觉。
   - 说"当前目录"之前先确认是哪个，别把上一条命令 `cd` 过去的地方当成当前目录。
   - 相对路径的输出（如 `grep -r .` 的结果）要还原成绝对路径再判断它属于谁。

3. **附加工作目录**（`/add-dir` 加进来的）同样是明确授权的范围，可读；但它们的父目录和兄弟目录不是。

## 服务器连接

> **Windows 主机操作注意事项**：通过 SSH 操作 W01 / W146 / W230 等 Windows 主机时，引号、路径、编码都有坑，详见 [`~/.claude/docs/windows-ssh-notes.md`](docs/windows-ssh-notes.md)。看到 `exit 255 + Connection closed` 先怀疑引号嵌套。

### 量化业务

| 代码 | SSH                                | 类型       | 备注                                                 |
| ---- | ---------------------------------- | ---------- | ---------------------------------------------------- |
| L129 | `ssh root@47.100.197.129`          | 阿里云     |                                                      |
| W146 | `ssh administrator@101.132.37.146` | 阿里云     | Windows Server 2022，量化回测/优化器，MySQL+Redis。**即将过期，业务迁往 W34** |
| W34  | `ssh administrator@47.101.52.34` | 阿里云     | **量化生产机（接替 W146）**，Windows Server 2025，MySQL 8.0+Anaconda+MATLAB，项目在 `D:\`。已配公钥免密（2026-09-13，密钥在 `C:\ProgramData\ssh\administrators_authorized_keys`）。详情见 `~/Documents/quant-home/asset/B_01_ECS.md` 解释表 1 |
| L103 | `ssh root@182.92.215.202`           | 阿里云     | 网站生产环境，非量化业务 |
| L68  | `ssh root@47.102.142.68`           | 阿里云     | CentOS 7，无法直连境外，已装 sing-box 代理（见下方） |
| W01  | `ssh -p 22332 administrator@lanyang.ddns.net` | 实体服务器 | Windows Server 2025，实体塔式，测试环境              |
| L01  | ssh -p 23458 hd@lanyang.ddns.net   | 实体主机   | 高性能主机，已装 sing-box 代理。**sudo 口令不落文档**——需要 sudo 时停下来向用户索取 |
| W230 | `ssh -p 47044 qw@9kfhg3026522.vicp.fun` | 实体主机   | 230 实体高性能主机，Windows，量化生产环境           |
| D01  | `ssh -p 42412 admin@lanyang.ddns.net` | 实体主机 | DGX GPU 服务器，Ubuntu aarch64，NVIDIA 内核          |

### 青云梯

| 名称 | SSH                          | 备注                                                            |
| ---- | ---------------------------- | --------------------------------------------------------------- |
| L207 | `ssh root@120.78.82.207`     | 生产外网服务器                                                  |
| Mskl | `ssh -p 5643 skl@180.142.110.214`    | 内网，远程 Mac Studio，Docker Compose 目录: /Users/skl/docker-compose（SSH 端口 2026-08-09 起改为 5643，非 22） |

### 武汉车企

| 名称  | SSH                              | 备注           |
| ----- | -------------------------------- | -------------- |
| Lhuin | `ssh -p 6189 root@47.114.56.83`  | 测试/开发服务器 |

### 个人

| 名称   | SSH                        | 备注 |
| ------ | -------------------------- | ---- |
| Lasher | `ssh root@121.41.177.150`  |      |