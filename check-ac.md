---
description: 检查用户故事验收条件(AC)的质量
argument-hint: [包含用户故事和AC的文件路径，或直接粘贴内容]
---

检查用户故事的验收条件（Acceptance Criteria）是否编写正确，基于上游推导链路和 Given/When/Then 规范进行多维度审查。

## 输入参数

- **source**：$ARGUMENTS（可以是文件路径、URL，或用户在对话中直接粘贴的内容）
- 如果未提供，询问用户提供待检查的用户故事和验收条件

## 前置要求

检查前，确认输入中包含以下内容（缺少则询问用户补充）：

| 必要信息 | 说明 | 缺失时的影响 |
|---------|------|------------|
| 用户故事 | As a / I want / So that 三段式 | 无法检查 AC 与故事的一致性 |
| 验收条件 | Given/When/Then 或规则列表 | 无检查对象 |
| 业务上下文（可选） | 整体解决方案、用户旅程 | 无法检查 AC 的上游可追溯性，降级为仅做格式和反模式检查 |

## 检查维度

### 维度 1：上游可追溯性检查

> 核心原则：验收条件是用户旅程和整体解决方案在当前故事语境下的细化。

逐条 AC 检查是否能追溯到上游：

- **Given** 是否来自用户旅程（用户当前处于什么阶段/状态）？
- **When** 是否来自整体解决方案（系统形态、交互方式）？
- **Then** 是否来自用户故事本身（要实现的功能价值）？

如果提供了业务上下文，逐条标注每个 AC 的 Given/When/Then 分别对应上游的哪个部分。如果未提供业务上下文，跳过此维度并在报告中注明。

### 维度 2：可测试性检查

对每条 AC 回答三个问题：

1. **怎么验证？** —— 能否明确描述验证步骤？
2. **边界在哪？** —— 是否有量化标准，而非模糊形容？
3. **反例是什么？** —— 能否说清"什么情况算不通过"？

三个问题中任何一个答不上来，该条 AC 标记为不合格。

### 维度 3：场景覆盖度检查

检查 AC 是否覆盖三条路径：

| 路径 | 要求 |
|------|------|
| 正常路径（Happy Path） | 用户按预期操作，一切正常 |
| 边界路径（Edge Case） | 极端值、空数据、首次使用、并发操作 |
| 异常路径（Error Path） | 网络异常、权限不足、数据异常、服务不可用 |

只有正常路径的 AC 一定是不够的。

### 维度 4：反模式检查

逐条 AC 排查以下反模式：

| 反模式 | 判定标准 | 严重程度 |
|--------|---------|---------|
| 含糊形容词 | 出现"快速"、"友好"、"合理"、"良好"等无法量化的词 | 严重 |
| 混入实现方案 | 出现具体技术选型（"用 Redis"、"调 XX 接口"） | 严重 |
| 遗漏主语 | Given/When/Then 缺少明确的操作主体 | 中等 |
| 缺少量化 | 涉及数量、时间、性能但无具体数值 | 中等 |
| 复述用户故事 | AC 只是把用户故事换了个说法，没有增加信息 | 严重 |
| Given 缺少前置状态 | Given 没有描述清楚系统/用户的初始状态 | 中等 |
| Then 缺少可观测结果 | Then 描述的结果无法被观测或验证 | 严重 |

### 维度 5：数量与粒度检查

| AC 数量 | 判定 | 建议 |
|---------|------|------|
| 0 条 | 缺失 | 必须补充 AC |
| 1-2 条 | 过少 | 大概率只覆盖了 Happy Path，需要补充边界和异常场景 |
| 3-8 条 | 合理 | 正常范围 |
| 9+ 条 | 过多 | 用户故事本身可能太大，建议拆分故事 |

### 维度 6：故事边界检查

检查 AC 是否引入了超出当前用户故事范围的功能。如果某条 AC 描述的行为更适合作为独立的用户故事存在，标记出来并建议拆分。

## 输出格式

### 总评

用一句话概括 AC 的整体质量，给出评级：

- **A（优秀）**：所有维度通过，可直接用于开发和测试
- **B（良好）**：存在少量可改进项，不影响整体理解
- **C（需修改）**：存在明显问题，需要修改后才能用于开发
- **D（需重写）**：根本性问题，建议回到上游重新梳理

### 逐条审查表

对每条 AC 输出：

```
AC #N: [AC 原文摘要]
├─ 可追溯性: ✅/⚠️/❌ [对应上游的哪个部分]
├─ 可测试性: ✅/⚠️/❌ [能否直接变成测试用例]
├─ 反模式:   ✅/⚠️/❌ [命中了哪些反模式]
└─ 建议:     [具体修改建议，如有]
```

### 覆盖度总览

```
正常路径: ✅/❌ (覆盖了 N 条)
边界路径: ✅/❌ (覆盖了 N 条)
异常路径: ✅/❌ (覆盖了 N 条)
```

如果有缺失，给出应该补充的具体场景建议。

### 修改建议

按优先级列出需要修改的项：

- **P0（必须修改）**：不可测试的 AC、复述用户故事的 AC
- **P1（建议修改）**：含糊形容词、缺少量化、遗漏主语
- **P2（可选优化）**：覆盖度不足、粒度可调整

对每条需要修改的 AC，给出修改前后的对比示例。

## 守护栏

- 不替用户编造业务上下文，不确定就问
- 不凭空添加 AC，只在"建议补充"中提供方向，具体内容需用户确认
- 如果用户故事本身有问题（如缺少 So that），先指出故事的问题，再检查 AC
- 检查基于已有信息，信息不足时明确标注"无法判断"而非猜测
- 保持人间清醒，不懂就问
