---
description: 分析一个项目的输入和输出，+ 核心算法或者处理流程
---

分析当前工作目录（或指定路径）的项目，产出一份「IO 流全景图」——搞清楚系统吃什么、吐什么、中间怎么嚼。

目标路径: $ARGUMENTS（未提供则使用当前工作目录）

## 分析维度

### I — 输入（系统吃什么）

| 类型 | 关注点 |
|------|--------|
| 数据源 | 数据库读取、文件读取、API 调用（作为客户端）、消息队列消费 |
| 配置 | 配置文件、环境变量、CLI 参数、硬编码常量/阈值 |
| 用户输入 | CLI 交互、Web 请求、UI 表单、定时触发 |

### O — 输出（系统吐什么）

| 类型 | 关注点 |
|------|--------|
| 数据写入 | 数据库写入、文件生成/导出、API 推送（作为客户端） |
| 响应 | API 响应（作为服务端）、CLI 输出、UI 渲染 |
| 副作用 | 通知（邮件/消息）、日志、外部系统状态变更 |

### F — 核心处理流（中间怎么嚼）

| 类型 | 关注点 |
|------|--------|
| 主管线 | 从入口到输出的主干调用链 |
| 关键算法 | 计算公式、评分模型、优化求解、统计聚合 |
| 数据变换 | 过滤、映射、聚合、格式转换、校验 |
| 决策逻辑 | 业务分支、路由选择、模式切换 |
| 编排/调度 | 步骤顺序、并行/串行、重试/回退、事务边界 |

## 执行流程

### Phase 1: 快速摸底（5 分钟）

1. 读取项目元信息：`README.md`、`CLAUDE.md`、`BUSINESS.md`、`pyproject.toml`、`package.json`、`pom.xml`、`Cargo.toml`、`go.mod`、`Makefile`、`docker-compose.yml` 等（有什么读什么）
2. 浏览目录结构（`tree` 或 `ls -R`），识别技术栈和项目类型
3. 确定分析策略：
   - **应用型**（Web 服务、CLI 工具、桌面应用）→ 从入口文件追踪
   - **库/SDK 型** → 从公开 API 表面追踪
   - **管线/脚本型**（数据管线、定时任务、自动化脚本）→ 从执行脚本追踪
   - **混合型** → 按子系统分别追踪

### Phase 2: 识别入口（5 分钟）

找到所有入口点：
- `main` 函数、`__main__.py`、`if __name__ == "__main__"`
- `run_*.py`、`start_*.sh`、CLI 命令定义
- Web 框架路由注册（Flask/FastAPI/Express/Spring 等）
- 定时任务配置（cron、Celery beat、APScheduler 等）
- 事件监听器、消息消费者

列出入口清单，标注每个入口的触发方式。

### Phase 3: 追踪 I/O（10 分钟）

从每个入口出发，沿调用链追踪：

**追踪输入：**
- 搜索数据库读取模式（`SELECT`、ORM `.query`/`.filter`/`.find`、`read_sql` 等）
- 搜索文件读取模式（`open()`、`read_csv`、`load`、`parse` 等）
- 搜索外部 API 调用（`requests.get`、`fetch`、`HttpClient` 等）
- 搜索配置加载（`yaml.load`、`dotenv`、`argparse`、`ConfigParser` 等）
- 搜索消息消费（`consume`、`subscribe`、`on_message` 等）

**追踪输出：**
- 搜索数据库写入模式（`INSERT`/`UPDATE`/`DELETE`、ORM `.save`/`.create`/`.commit` 等）
- 搜索文件写入模式（`to_csv`、`write`、`export`、`dump` 等）
- 搜索外部 API 推送（`requests.post`、`fetch` with POST、`send` 等）
- 搜索通知/消息发送（`send_email`、`notify`、`publish` 等）

### Phase 4: 提取核心处理流（10 分钟）

对每条主要的输入→输出管线：

1. **追踪主干调用链**：从入口到输出，记录关键函数/方法的调用顺序
2. **识别关键算法**：查找包含核心计算逻辑的函数（公式、循环处理、条件分支密集区）
3. **记录数据变换**：数据在管线中的形态变化（原始→清洗→计算→输出）
4. **标注决策点**：业务分支、模式切换、异常处理

### Phase 5: 生成报告

## 产出格式

输出为 Markdown，结构如下：

```markdown
# IO 流全景图: [项目名]

> 分析日期: YYYY-MM-DD HH:MM
> 项目路径: [path]
> 技术栈: [language / framework / key deps]
> 项目类型: 应用型 / 库型 / 管线型 / 混合型

## 1. 入口清单

| # | 入口文件 | 触发方式 | 说明 |
|---|---------|---------|------|

## 2. 输入清单

| # | 输入名称 | 类型 | 来源 | 格式/协议 | 消费者（代码位置） |
|---|---------|------|------|----------|-----------------|

## 3. 输出清单

| # | 输出名称 | 类型 | 去向 | 格式/协议 | 生产者（代码位置） |
|---|---------|------|------|----------|-----------------|

## 4. 核心处理流

### P-01: [管线名称]

**一句话摘要：** [输入X] → [经过什么处理] → [输出Y]

**流程步骤：**
1. [步骤1] — `file.py:line` — [做什么]
2. [步骤2] — `file.py:line` — [做什么]
...

**关键算法/逻辑：**
- [算法名称]: [一句话描述算法思路] — `file.py:line`
- [决策点]: [分支条件和影响] — `file.py:line`

**数据变换链：**
原始数据 (DataFrame/JSON/...) → [变换1] → [变换2] → 输出格式

---
（重复 P-02 ... P-0N）

## 5. IO 连接图（文本）

用 ASCII 或简单文字描述输入→处理→输出的全局连接关系：

[输入A] ──→ ┌──────────┐ ──→ [输出X]
[输入B] ──→ │ 处理流 P-01 │ ──→ [输出Y]
             └──────────┘
[输入C] ──→ ┌──────────┐ ──→ [输出Z]
             │ 处理流 P-02 │
             └──────────┘

## 6. 统计

| 维度 | 数量 |
|------|------|
| 入口 | N |
| 输入源 | N |
| 输出目标 | N |
| 核心处理流 | N |

## 7. 观察与疑问

- [分析过程中发现的值得注意的点]
- [不确定的地方，标"待确认"]
```

## 输出方式

分析完成后，**必须自动将报告保存为 Markdown 文件**，无需用户额外要求：

1. **文件命名**: `io-flow_{项目目录名}_{YYYY-MM-DD}_{HHmm}.md`
   - 项目目录名 = 当前工作目录的最后一级目录名
   - 日期时间 = 分析时的本地时间
   - 示例: `io-flow_v3-signal_valuation_factor_2026-04-12_1746.md`
2. **保存位置**: 项目根目录（当前工作目录）
3. **报告标题**: 在 `# IO 流全景图: [项目名]` 的 blockquote 中包含精确到分钟的分析时间：
   ```
   > 分析日期: YYYY-MM-DD HH:MM
   ```
4. **完成后告知用户**: 打印保存路径

## 守护栏

- **追踪主干，不穷举** — 只追 3-10 条核心管线，不逐行扫描每个函数
- **标注代码位置** — 每个发现都附 `file:line`，方便用户验证
- **不确定标"待确认"** — 不编造，看不清的如实说
- **不改任何代码** — 纯分析，只读
- **语言无关** — 按实际技术栈调整搜索模式，不限于特定语言
- **深度适配规模** — 小项目（<20 文件）可以更细致；大项目（>100 文件）只追核心链路
- **先展示再确认** — 产出后等用户反馈，用户补充信息后可迭代
