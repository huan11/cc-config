---
description: W34 上跑任何程序之前，先出一张「运行前信息表」给人确认。单次采集（一条命令 ~9 秒拿全量），只看统一代码根 D:\00-v5-latest-code\。出表后必须停下等确认，不得自行开跑。
argument-hint: <程序名>　例：fio ｜ TotalDB ｜ v4-demo-fund-etl ｜ email-downloader
allowed-tools: Bash, Read
---

**W34 上要跑 `$ARGUMENTS` 之前的确认闸门。**

## 定位

用户让 AI 在 W34 上跑程序时，AI **先**出这张表，用户看完确认，**再**开跑。

1. **只采集，不运行业务。** 采集脚本是只读的；额外允许 `--help` / `--dry-run` / `preflight`。**禁止**真实业务、写库、启用计划任务。
2. **出表即停。** 表出完就结束，明说"等你确认后再跑"。**不要**顺手把程序跑掉。
3. **表要让人能否决。** 每行都得支撑"该不该跑、跑什么参数"的判断，不是罗列现状。

---

## 🔴 约束（这些是硬边界，不是建议）

### 1. 只看统一代码根

**`D:\00-v5-latest-code\<程序名>`。找不到就是找不到，不去别处搜。**

D_04 定的统一代码根就是这个路径。程序不在这里 = **违反部署约定**，这本身就是要报给人的结论，不是需要 AI 绕过的障碍。

- 不在 → 表只出到「代码位置」这一行，标 🔴 **未在统一代码根**，给出建议动作（归位 clone / 由人明确指定路径），**然后停**
- 用户**显式**给了别的路径 → 可以查，但必须在表里标 🔴「不在统一代码根（例外）」
- **不要**自作主张去探 `D:\<项目>`、`D:\fund-sync-*\`、`D:\00-v4-latest-code\` 等位置

### 2. 一次采集，不要交互式逐条查

```bash
~/.claude/scripts/w34-run-info.sh $ARGUMENTS
```

一条命令输出完整 JSON（实测 ~9 秒），内含：机器/内存/磁盘、git 全量、所有 venv、配置文件清单+时间戳、env 键值（敏感键自动打码）、匹配的计划任务、日志/状态/收据目录、入口候选。**解析这个 JSON 填表**。

采集器在 `~/.claude/scripts/w34-run-info-collect.ps1`，wrapper 已内置 ControlMaster 连接复用。

🔴 **不要**回退成十几次 `sshpass ssh` 逐项查——那是本命令上一版的做法，慢在往返次数上，不是网络。
🔴 **不要**加预防性 `sleep`。ControlMaster 已消除"连续 SSH 被拒"；真被拒了才重试一次。

**JSON 不够时**才补查，且每次补查必须是一条命令拿多项。

### 3. 时间预算与降级

**最多 3 次 SSH 往返 / 90 秒。** 超了就出表，缺的标 `🟡 未取到` + 原因。**不要为了填满一行无限挖下去。**

### 4. 不做排障

运行前检查**只回答"能不能跑、跑什么参数"**。发现矛盾（如水位有记录但产物目录为空）**记录并交给人**，不追查成因。想排障另开一轮。

### 5. 其它铁律

- **一切实拉。** 台账天天过期。禁止凭记忆、凭台账、凭上一轮结论填表。
- **禁止打印凭据。** 采集器已对 `PASSWORD|SECRET|TOKEN|WEBHOOK|KEY|CREDENTIAL|PASS` 打码；补查时同样只输出键名。路径、库名、主机名不是凭据。
- **拿不到写 `🟡 未取到` + 原因。** 不留空、不编。
- **推测标注"推测"。**
- **只读。** 采集器会 `git fetch`（只读）；**`git pull` 不做**——机器上可能有人在工作。

---

## 补查时的编码与命令规则（写死，不现场试错）

| 场景 | 固定做法 |
|---|---|
| 读**文件内容** | 远端 `chcp 65001 >nul` + `type`，本地**不要**再 iconv |
| 读**命令输出** | 本地 `iconv -f GBK -t UTF-8` |
| 判成败 | **不加管道**（管道返回的是最后一段的退出码，会掩盖远端失败） |
| 路径 | 用 `git -C <无引号路径>`，**不要 `cd`**（`for` + 引号会崩） |
| 条件判断 | `if exist X (...) else (...) & 后续` 的 `&` 会被 else 吞掉——**一条命令查一件事** |
| 复杂逻辑 | 写进 `.ps1`，base64 上传执行；**PowerShell 里不写中文字符串**（GBK 截断报 `TerminatorExpectedAtEndOfString`） |
| 硬件/服务 | `wmic` 在 Server 2025 已移除，用 PowerShell |
| 本地 zsh | `$var:q` 会被当参数修饰符吃掉，拼 `gh-${r}:owner/repo` 要用 `${}` 界定 |

W34 环境：`ssh administrator@47.101.52.34`（口令见 `~/Documents/quant-home/account/`），默认 shell `cmd.exe`，主机名 `iZt4y7nrxnvk0hZ`，8C/32G（31.5 GB 可见内存），git 在 PATH，MATLAB 在 `C:\ProgramFiles\MATLAB\R2025b\bin\matlab.exe`，⚠️ **2026-09-25 到期未续费**。

---

## 主表（行序固定）

```
# <项目> 运行前信息表（<分支>）

| 项 | 值 |
|---|---|
| 机器 | W34（<hostname>）｜ 空闲内存 X / Y GB ｜ D: 剩 Z GB |
| 代码位置 | D:\00-v5-latest-code\<项目> ✅ 在统一根 |
| 分支 | <branch>（tracking <upstream>；behind N / ahead N） |
| 最新 commit | <短 sha> · <commit 消息原文> |
| 提交人 | <name> <email> |
| 提交日期 | <iso> |
| 相对上次运行版本 | 前进 N 个提交（原 <sha>, 日期, 提交人） |
| 工作区 | 干净 / N 个文件被改 |
| 拉码方式 | <remote url>（别名覆盖范围） |
| 环境 | <venv 列表 + Python 版本>；uv 是否可用 |
| 离线验收 | <preflight / --help 结果> |
| 配置目录 | <N 个文件，最后修改时间，是否随代码更新> |
| 读 | <实例>: <库/源> |
| 写 | <实例>: <库> / <文件路径> |
| 上次成功 | <日期 周期>（依据） |
| 上次失败 | <日期> —— 卡在 <段> |
| 建议运行日期 | <日期>（依据） |
| 周期 | <p1 / daily / hourly> |
| 调度状态 | <任务名 + Trigger + 状态>；无任务写「无，靠人工触发」 |
```

**判读要点**（JSON 里有数据，判断要你给）：

| 行 | 判读 |
|---|---|
| 代码位置 | 🔴 不在统一根 → 违反部署约定，表止于此 |
| 分支 | 🔴 特性/个人分支（`codex/*`、`feat/*`、`mh-db-clean`）跑生产要标出 |
| 最新 commit | 消息**照抄**不概括 |
| 相对上次运行版本 | ⚠️ 收据里取不到上次 commit 就写 `🟡 未取到`，**别拿 origin 差距冒充** |
| behind N | 🔴 要跑的不是最新码。**看 `behind_files`**——只改文档 vs 改了代码/配置模板，结论完全不同 |
| 环境 | 🔴 无 venv 或 uv 缺失 → 跑不起来，表到此为止。注意 venv 可能在子目录（`repos\*`、`runtime\*`） |
| 配置目录 ⭐ | ⭐ **最隐蔽的坑**：配置修改时间**早于**代码提交日期 → 标「**未随代码更新**」。与 `*.example` **比键名不比值**，缺哪些键列出来 |
| 写 | 🔴 写目标是生产库要标出；影子/隔离库（`tmp` / `_mh` / `tmp_<host>`）标明 |
| 调度状态 | 🔴 无任务 = 未真正上线；`Disabled` 要标出。**核 action 指向的路径是否 = 统一根** |

---

## 开跑前需要人拍板（必出）

主表之后**必须**单列这节——这是本命令存在的理由。至少覆盖：

| 待定项 | 为什么问 |
|---|---|
| **业务日期** | 跑哪天的 T / P？补跑失败那期还是新日？**等价重放必须复用同一个 T** |
| **写目标库** | 会不会和别的机器/部署撞同一 schema？（实测 W34 与 W146 都写 `rds01.tmp`） |
| **资源占用** | 预计耗时与内存峰值 vs 当前空闲；现在适合占机器吗？与哪些 Ready 任务并发？ |
| **通知** | 会不会往飞书真发消息？测试跑建议 `--no-notify` |
| **跑哪份代码** | behind 时跑当前还是先更新？（**更新要人点头，本命令不 pull**） |
| **参数陷阱** | 该程序已知的坑（例：`fio` 的 `--data-date` 与 `--valuation-date` **必须成对**，缺一个直接 `ValueError`） |

## 🔴 未闭合问题

逐条列，标 🔴/🟠/🟡，写清是**代码问题、配置问题还是数据问题**。

## 通用判读陷阱

| 现象 | 别误读 |
|---|---|
| 收据 `status: PASS` | ⚠️ **不足以证明真跑了**。复用与降级都显示为绿。要核 `business_daily_run_performed` / `database_mutation_performed` / `production_mutation_performed` / 复用标记 / 退出码 |
| 退出码 `3` | ⚠️ 很多套件里 `3` = **DEGRADED**，不是 PASS 也不是普通 FAILED；`if errorlevel 1` 会把它和 FAILED 混为一谈 |
| `Repository not found` | ⚠️ 是"**key 无权访问**"的标准回法，**不代表仓库不存在** |
| 告警 `pending` 堆积 | ⚠️ 告警投递通常与业务执行**刻意分离**，投递失败不改退出码 → 「日更报 PASS 但没人收到告警」 |
| `--help` 跑通 | ✅ 本身就是"私有配置自动加载成功"的证据，可当离线验收一项 |
| 环境变量 | ⚠️ 部分项目（如 `fio`）**刻意取消**了 `FOUR_PROJECT_SUITE_*`，改由项目内配置提供。**不要 `setx`**；旧文档的 `set VAR=...` 别照抄 |
| remote 写了别名 | ⚠️ 不等于别名存在——要核 `~/.ssh/config` 真有那个 Host（实测遇到 remote 是 `gh-TotalDB:` 但 config 无此条目） |
| 非 git 目录 | 🟠 无版本追溯，git 那几行填 `⚠️ 非 git`，改报目录大小与配置是否存在 |

## 收尾

表出完 → **停下**，说明"确认后再跑"，给出**建议的启动命令**（有 dry-run 的先给 dry-run）供人过目。

**不要**在未确认前执行业务命令。**不要**顺手改台账——需要同步 `~/Documents/quant-home/asset/` 时先问。
