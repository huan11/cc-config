---
description: 扫描当前项目运行赖以依存的所有准备工作（环境变量、外部软件、数据库、配置文件、数据源、共享库、端口网络、定时任务、OS/硬件），生成完整的运行前提清单。新人接手、环境搭建、跨机器部署时使用。
---

扫描目标路径下所有代码，穷举该项目能跑起来所需的**全部前提条件**，生成运行前提清单。

目标路径: $ARGUMENTS

如果未提供路径，使用当前工作目录。

## 你的角色

你是一名**部署前审计员**。你的任务不是理解业务逻辑，而是回答一个问题：**如果我拿到这份代码，我需要准备什么才能让它跑起来？**

## 扫描维度（11 类）

### 1. 环境变量

搜索代码中所有 `os.environ`、`os.getenv`、`%ENV_VAR%`（BAT）、`$ENV_VAR`（shell）、`.env` 引用。

对每个环境变量记录：
- 变量名
- 在哪个文件中引用（file:line）
- 用途推断（路径？凭据？开关？）
- 是否有默认值 / fallback

### 2. 外部软件

搜索代码中对外部可执行程序的调用：`subprocess`、`os.system`、`matlab.engine`、BAT 中的 `call`、`python`、`matlab` 等。

对每个外部软件记录：
- 软件名 + 版本要求（如能推断）
- 调用方式（Python API / CLI / BAT）
- 在哪个文件中引用

### 3. Python 包依赖

读取 `requirements.txt`、`pyproject.toml`、`setup.py`、`setup.cfg`。如果没有依赖声明文件，扫描所有 `import` / `from ... import` 语句，过滤掉标准库，列出第三方包。

标注：是否存在依赖声明文件？声明的包和实际 import 的包是否一致？

### 4. 数据库连接

搜索所有数据库连接相关代码：`pymysql.connect`、`sqlalchemy.create_engine`、`DATABASES`、连接字符串、SQL 配置文件（`.yaml`、`.json`）。

对每个连接记录：
- 数据库类型（MySQL / PostgreSQL / SQLite 等）
- 连接信息来源（硬编码 / 配置文件 / 环境变量）
- schema / database 名
- 是否包含明文凭据（安全风险标记）

### 5. 配置文件

搜索代码中读取的所有配置文件（`json.load`、`yaml.safe_load`、`pd.read_excel`、`configparser`、`open(...config...)`）。

对每个配置文件记录：
- 文件路径（绝对 / 相对）
- 格式（JSON / YAML / Excel / INI）
- 是否在版本控制中（检查 .gitignore 和实际文件是否存在）
- 如果不在版本控制中，标记为 **需手动准备**

### 6. 外部数据源 / API

搜索 HTTP 请求（`requests.get/post`、`urllib`、`httpx`）、SDK 调用（Wind API、Tushare、聚源等金融数据终端）、Webhook 地址。

对每个数据源记录：
- 数据源名称
- 认证方式（token / 账号密码 / 终端登录）
- 在哪个文件中使用

### 7. 文件系统依赖

搜索代码中读写的文件路径，特别是：
- 硬编码的绝对路径（`C:\`、`/data/`、`/home/` 等）
- 必须预先存在的输入目录 / 文件
- 代码假设存在但不自动创建的输出目录

对每个路径记录：
- 路径
- 读/写/both
- 是否硬编码
- 在哪个文件中引用

### 8. 内部共享库

搜索 `sys.path.append`、`sys.path.insert`、通过环境变量引入的私有包（如 `global_tools`）。

对每个共享库记录：
- 库名
- 引入方式（环境变量 / sys.path / 相对路径）
- 提供了哪些关键函数（从 import 语句推断）
- 该库是否在当前仓库内

### 9. 端口与网络

搜索代码中绑定或连接的端口号：`socket.bind`、`listen`、`connect`、Flask/FastAPI `app.run(port=)`、数据库连接串中的端口、配置文件中的 `port` / `host` 字段。

对每个端口记录：
- 端口号
- 协议（TCP / HTTP / WebSocket）
- 方向（监听 / 连接）
- 关联服务（MySQL 3306 / Wind 终端 / 飞书 Webhook 等）

同时检查网络层面依赖：
- 是否需要 VPN / 内网访问（从 IP 地址段推断，如 192.168.x.x / 10.x.x.x）
- 是否有防火墙相关提示（注释或文档）
- 外发 HTTP 请求的目标域名清单

### 10. 定时任务 / 调度

搜索所有与调度相关的配置：
- BAT/Shell 脚本中的 `schtasks`、`crontab`、`at` 命令
- Python 中的 `schedule`、`APScheduler`、`time.sleep` 循环
- 代码注释 / 文档中提到的定时触发时间
- `*.bat` / `*.sh` 文件本身（作为 Windows 计划任务 / cron 的入口）

对每个调度项记录：
- 触发时间 / 频率
- 执行什么脚本/函数
- 调度方式（Windows 计划任务 / cron / 代码内循环）

### 11. 操作系统与硬件

从代码中推断运行环境要求：
- **OS 要求**：BAT 脚本 → Windows；shebang → Linux/macOS；`platform` / `sys.platform` 判断
- **软件许可证**：MATLAB License、Wind 终端授权、商业数据库等付费软件
- **CPU/内存**：`multiprocessing.Pool` / `ProcessPoolExecutor` 的进程数（推断 CPU 核心需求）；大数据集内存加载（pandas 大文件读取）
- **磁盘空间**：大量 CSV/Excel 输出、日志文件、历史数据存储
- **字符编码**：`encoding='gbk'` / `encoding='gb2312'` / `encoding='utf-8'`（标注编码要求，尤其中文环境）

## 输出格式

写入 `docs/setup-guide.md`，按以下结构组织：

```markdown
# 运行前提清单: [项目名]

> 扫描日期: YYYY-MM-DD
> 扫描路径: [path]

---

## 一、硬性前提（缺一不可）

<!-- 从 11 个维度中提炼出"没有就跑不起来"的条目，按启动顺序排列 -->
<!-- 判定标准：代码中直接引用且无 fallback，缺失会导致 ImportError / ConnectionError / FileNotFoundError / 启动崩溃 -->

| # | 类别 | 必需项 | 说明 | 来源（file:line） |
|---|------|--------|------|-------------------|
| 1 | OS | 例: Windows | BAT 脚本无法在 Linux 运行 | *.bat |
| 2 | 外部软件 | 例: MATLAB R2018b+ | matlab.engine 调用 | call_matlab_opt.py:XX |
| 3 | 软件许可证 | 例: MATLAB License | 无许可证引擎无法启动 | — |
| 4 | 环境变量 | 例: GLOBAL_TOOLSFUNC_new | 无默认值，启动即崩 | global_dic.py:XX |
| 5 | 内部共享库 | 例: global_tools | 全模块依赖 | 多处 import |
| 6 | 数据库 | 例: MySQL tracking_new | 读写均依赖 | XX.py:XX |
| 7 | 配置文件 | 例: trading_path_config.json | 不在版本控制中 | global_dic.py:XX |
| ... | ... | ... | ... | ... |

**检验方式**：新机器上按此表逐项检查，全部 ✅ 后程序应能正常启动（不含业务数据）。

---

## 二、速查总览（11 维度统计）

| 类别 | 数量 | 状态 |
|------|------|------|
| 环境变量 | X 个 | Y 个无默认值 |
| 外部软件 | X 个 | |
| Python 包 | X 个 | 依赖声明文件: 有/无 |
| 数据库连接 | X 个 | Y 个含明文凭据 |
| 配置文件 | X 个 | Y 个不在版本控制中 |
| 外部数据源 | X 个 | |
| 文件系统路径 | X 个硬编码 | |
| 内部共享库 | X 个 | |
| 端口/网络 | X 个端口 | 内网/公网/VPN |
| 定时任务 | X 个 | 调度方式 |
| OS/硬件 | 目标 OS | 许可证 X 个 |

---

## 三、配置与连接全景（重构基线）

<!-- 系统性整理当前系统使用的全部配置项和连接信息，作为重构对比的基线快照 -->

### 3.1 数据库连接清单

| 连接名 | 类型 | Host | Port | Database/Schema | 用户名 | 密码来源 | 读/写 | 使用模块 |
|--------|------|------|------|-----------------|--------|---------|-------|---------|
| 例 | MySQL | 192.168.x.x | 3306 | tracking_new | root | 硬编码 | R/W | Tracking |

### 3.2 配置文件清单

| 文件路径 | 格式 | 在版本控制中 | 关键字段 | 使用模块 |
|---------|------|------------|---------|---------|
| 例: trading_path_config.json | JSON | 否 | sql_host, data_path, ... | Trading |

### 3.3 环境变量清单

| 变量名 | 当前用途 | 默认值 | 引用位置 |
|--------|---------|--------|---------|

### 3.4 外部服务连接

| 服务名 | 协议 | 地址/端口 | 认证方式 | 用途 | 使用模块 |
|--------|------|----------|---------|------|---------|
| 例: Wind 终端 | COM/API | localhost | 终端登录 | 行情数据 | Data_update |
| 例: 飞书 Webhook | HTTPS | open.feishu.cn | URL 含 token | 告警通知 | Trading, Tracking |
| 例: Tushare | HTTPS | api.tushare.pro | token | 指数/期权数据 | Data_update |

### 3.5 定时任务时间表

| 时间 | 执行内容 | 入口文件 | 调度方式 |
|------|---------|---------|---------|
| 例: 07:05 | 市场数据更新 | update_main.bat | Windows 计划任务 |

### 3.6 文件系统路径映射

| 逻辑名称 | 实际路径 | 来源 | 读/写 | 使用模块 |
|---------|---------|------|-------|---------|
| 例: data_path | C:\quant\data | config.json | R | 全模块 |

---

## 四、搭建步骤（建议顺序）

<!-- 根据扫描结果，推断一个合理的环境搭建顺序 -->

1. 确认 OS 和硬件
2. 获取软件许可证（MATLAB / Wind / 聚源等）
3. 安装外部软件
4. 安装 Python 包
5. 设置环境变量
6. 准备配置文件（从「配置与连接全景」中逐项核对）
7. 建立数据库连接 + 开放端口
8. 部署内部共享库
9. 创建目录结构
10. 配置数据源认证
11. 配置定时任务
12. 网络 / 防火墙

---

## 五、详细清单（11 维度逐项展开）

### 5.1 环境变量
| 变量名 | 用途 | 引用位置 | 有默认值 |
|--------|------|---------|---------|

### 5.2 外部软件
...（按维度逐一展开）

---

## 六、风险项

<!-- 扫描中发现的安全或可靠性问题 -->
- 明文凭据
- 缺失的依赖声明文件
- 不在版本控制中的关键配置
```

## 执行策略

1. **先广后深** — 先用 Grep/Glob 快速扫完 11 个维度的关键模式，再逐条确认
2. **并行扫描** — 11 个维度之间无依赖，尽量并行搜索
3. **硬性前提优先** — 扫描完成后，先从全部结果中筛选出「缺了就崩」的硬性前提，置于报告最顶部
4. **全景板块要穷举** — 「配置与连接全景」不是采样，是完整清单。每个数据库连接、每个配置文件、每个外部服务都要列出，不遗漏
5. **只报事实** — 扫到什么报什么，不确定的标"待确认"，不编造
6. **排除噪音** — 忽略 `.git`、`__pycache__`、`*.pyc`、`node_modules`、注释中的路径示例

## 守护栏

- **目标仓库只读** — 只写 `docs/setup-guide.md`，不修改任何已有文件
- **不执行代码** — 只做静态扫描，不运行任何脚本
- **不遗漏比误报好** — 宁可多列一条"待确认"，也不漏掉一个真实依赖
- **区分"代码中引用"和"实际需要"** — 如果某段代码被注释掉或明显废弃，标注但不算入主清单
- **产出后等确认** — 展示速查总览，等用户确认再结束
