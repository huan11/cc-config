---
description: 扫描代码中引用但仓库内不存在的配置项（配置文件、环境变量、共享库、数据库凭据等），生成一份可以直接拿去问人的"待确认清单"。接手新项目、跨机器部署、排查启动失败时使用。
---

扫描目标路径，找出所有**代码引用了但仓库里没有**的配置项，生成待确认清单。

目标路径: $ARGUMENTS

如果未提供路径，使用当前工作目录。

## 你的角色

你是一名**配置侦探**。你的任务是回答一个问题：**这份代码里有哪些东西是我自己搞不定、必须找人问的？**

你不需要理解业务逻辑，只需要找到代码和现实之间的缺口。

## 扫描范围（6 类缺口）

### 1. 缺失的配置文件

搜索代码中读取配置文件的语句：
- `yaml.safe_load` / `yaml.load` + `open(...)`
- `json.load` + `open(...)`
- `pd.read_excel(...)` 用于读取配置（非数据）
- `configparser`
- `open(.*config.*)`

对每个配置文件：
- 从代码中提取文件路径（绝对路径 / 相对路径 / 通过变量拼接的路径）
- **检查该文件是否实际存在于仓库中**（`ls` / `Glob` 验证）
- 如果不存在，列入清单，标注：
  - 文件路径（代码中的引用方式）
  - 格式（YAML / JSON / Excel）
  - 代码中读取了哪些字段/sheet（如能推断）
  - 引用位置（file:line）

### 2. 缺失的环境变量

搜索 `os.getenv`、`os.environ`、BAT 中的 `%VAR%`、shell 中的 `$VAR`。

对每个环境变量：
- 检查是否有默认值 / fallback
- 如果无默认值 → 列入清单
- 标注：变量名、用途推断、引用位置

### 3. 缺失的内部共享库

搜索 `sys.path.append` / `sys.path.insert` 引入的路径，以及通过环境变量加载的私有模块。

对每个共享库：
- 检查该库是否在当前仓库内
- 如果不在 → 列入清单
- 从 `import` 语句推断该库提供的关键函数清单

### 4. 未配置化的数据库连接

搜索 `pymysql.connect`、`create_engine`、连接字符串。

区分两种情况：
- **硬编码连接**（host/user/password 直接写在代码里）→ 列入清单，标注"需确认是否为当前有效凭据"
- **配置文件连接**（从 YAML/JSON 读取）→ 如果配置文件本身缺失，归入"缺失的配置文件"

### 5. 间接引用的配置（二级依赖）

有些配置文件本身是一个路径注册表（如 Excel 路径映射表），代码通过它获取其他路径。

- 搜索代码中的路径注册/字典模式（如 `glv.get('key')`、`config['path']`）
- 穷举所有注册的 key 名
- 标注：这些 key 的实际值定义在哪个配置文件中，该文件是否存在

### 6. 无法自查的外部依赖

- 数据文件目录（代码假设某个目录下有按日期命名的 CSV/Excel，但目录内容无法从代码推断）
- 定时任务配置（代码是 BAT 脚本，但 Windows 计划任务的配置不在代码里）
- 网络/防火墙规则（代码连接外部 IP/域名，需确认网络可达性）

## 输出格式

写入 `docs/missing-config-[项目名].md`（文件名带项目后缀），结构如下：

```markdown
# 待确认清单: [项目名]

> 扫描日期: YYYY-MM-DD
> 扫描路径: [path]
> 判定标准: 代码中引用但仓库内不存在，需要找人确认或索取

---

## 快速统计

| 类别 | 缺失数量 | 阻断级别 |
|------|---------|---------|
| 配置文件 | X 个 | 缺了启动崩溃 |
| 环境变量 | X 个 | 缺了启动崩溃 |
| 内部共享库 | X 个 | 缺了 ImportError |
| 数据库凭据 | X 个 | 需确认有效性 |
| 间接引用配置 | X 个 | 依赖上游配置文件 |
| 外部依赖 | X 个 | 需人工确认 |

---

## 零、需要提供的配置文件清单

<!-- 在报告最开头列出所有需要提供的配置文件，附带仓库内应存放的路径，方便用户直接对照准备 -->

| # | 文件名 | 应存放路径（相对仓库根目录） | 格式 | 优先级 | 状态 |
|---|--------|--------------------------|------|-------|------|
| 1 | 例: data_update_path_config.xlsx | `config_path/data_update_path_config.xlsx` | Excel | P0 | 缺失 |
| 2 | 例: sql_connection.yaml | `config_project/Data_update/sql_connection.yaml` | YAML | P0 | 缺失 |
| ... | ... | ... | ... | ... | ... |

> 请按此清单逐个提供文件，放到对应路径即可。拿到 P0 文件后，P1 和间接依赖的问题大部分会自动解决。

---

## 一、找谁要 & 要什么（按优先级排列）

<!-- 按"缺了的后果严重程度"排序，最致命的排最前 -->
<!-- 每个条目都要可操作：拿着这个条目就能去问人 -->

### P0: 没有就完全跑不起来

| # | 要什么 | 格式 | 要问的问题 | 代码引用 |
|---|--------|------|-----------|---------|
| 1 | 例: data_update_path_config.xlsx | Excel | "这个文件在哪？有几个 sheet？能给我一份模板吗？" | global_dic.py:40 |
| 2 | 例: GLOBAL_TOOLSFUNC_new 的值 | 环境变量 | "这个环境变量在生产机器上指向什么路径？global_tools 源码在哪？" | 全模块 |

### P1: 没有会部分功能失败

| # | 要什么 | 格式 | 要问的问题 | 代码引用 |
|---|--------|------|-----------|---------|

### P2: 需要确认但不阻断启动

| # | 要什么 | 确认内容 | 代码引用 |
|---|--------|---------|---------|
| 1 | 例: MySQL 密码 <口令已移除> | "这个密码还有效吗？是否应该迁移到配置文件？" | Mktdata_preparing.py:380 |

---

## 二、缺失配置文件详情

<!-- 每个缺失文件一个小节 -->

### [文件名/路径]
- **代码引用**: file:line
- **格式**: YAML / JSON / Excel
- **代码中读取的字段/sheet**: ...
- **影响范围**: 哪些模块会因为缺这个文件而失败
- **建议问法**: "能给我一份 [文件名] 的模板或示例吗？它需要包含这些字段: ..."

---

## 三、缺失环境变量详情

| 变量名 | 用途 | 无默认值 | 引用位置(数量) | 建议问法 |
|--------|------|---------|--------------|---------|

---

## 四、缺失共享库详情

| 库名 | 引入方式 | 提供的关键函数 | 建议问法 |
|------|---------|-------------|---------|

---

## 五、数据库凭据确认

| 连接标识 | Host | DB | User | 密码方式 | 建议问法 |
|---------|------|-----|------|---------|---------|

---

## 六、间接引用配置（二级依赖）

<!-- 代码通过某个"路径注册表"间接引用的配置 -->
<!-- 只有拿到上游配置文件后才能知道这些路径指向哪里 -->

| 注册表 | key 名 | 用途推断 | 代码引用 |
|--------|--------|---------|---------|

---

## 七、外部依赖确认

| 依赖项 | 类别 | 建议问法 | 代码引用 |
|--------|------|---------|---------|
```

## 执行策略

1. **先扫后验** — 先用 Grep 搜出所有配置引用，再用 Glob/ls 逐个验证是否存在
2. **路径追踪** — 配置文件路径可能是拼接的（`os.path.join(base, 'config', 'xxx.yaml')`），需要追踪变量来源
3. **区分配置读取和数据读取** — `pd.read_excel` 可能是读配置也可能是读业务数据，根据上下文（变量名含 config/setting/path、文件名含 config）判断
4. **二级依赖要标注** — 如果配置 A 定义了配置 B 的路径，而 A 本身就缺失，那 B 的所有 key 都是"间接缺失"，归入第六节
5. **问法要具体** — 不要写"请提供配置文件"，要写"能给我一份 sql_connection.yaml 的模板吗？需要包含 host, port, user, password, database 这 5 个字段"

## 守护栏

- **只写 `docs/missing-config-[项目名].md`**，不修改任何已有文件
- **不执行代码** — 纯静态扫描
- **不猜测配置值** — 只报"缺了什么"，不猜"应该填什么"
- **区分注释和活代码** — 注释掉的引用标注为"已注释，可能废弃"
- **产出后展示快速统计表**，等用户确认
