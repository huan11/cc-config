---
description: 检查用户故事的质量（含 So that 价值陈述和验收条件）
argument-hint: [包含用户故事的文件路径，或直接粘贴内容]
---

检查用户故事的整体质量，包括故事卡本身、So that 价值陈述、验收条件，以及与上游（角色目标、整体解决方案、用户旅程）的一致性。

## 输入参数

- **source**：$ARGUMENTS（可以是文件路径、URL，或用户在对话中直接粘贴的内容）
- 如果未提供，询问用户提供待检查的用户故事

## 前置信息收集

检查前，确认输入中包含以下内容：

| 信息 | 必要性 | 缺失时处理 |
|------|--------|-----------|
| 用户故事（As a / I want / So that） | 必须 | 无法检查，要求用户提供 |
| 验收条件（AC） | 强烈建议 | 标记为"AC 缺失"，仅检查故事卡部分 |
| 业务上下文（整体解决方案、用户旅程） | 可选 | 无法做上游追溯检查，降级为格式和反模式检查，在报告中注明 |
| 相关的其他用户故事 | 可选 | 无法做边界和独立性检查，在报告中注明 |

## 检查流程

### 第 1 步：故事卡格式检查

检查 As a / I want / So that 三段是否完整：

| 检查项 | 判定标准 |
|--------|---------|
| As a（角色） | 是否是具体的用户角色，而非"用户"、"使用者"等泛称 |
| I want（功能） | 是否描述了一个具体的行为或功能，而非技术实现 |
| So that（价值） | 是否存在且非空洞（见第 2 步详细检查） |

### 第 2 步：So that 价值陈述检查

用户故事的 So that 只有三种有效写法，逐一比对：

| 类型 | So that 指向 | 判定方式 |
|------|-------------|---------|
| 满足角色目标 | 回溯到用户角色的核心目标 | So that 描述的是用户最终想达成的目的 |
| 满足规则/流程 | 回溯到整体解决方案中的业务规则或流程约束 | So that 描述的是某个业务规则或合规要求 |
| 进行旅程下一步 | 回溯到用户旅程中的前后衔接 | So that 描述的是完成当前步骤后能进入下一步 |

**反模式检查：**

| 反模式 | 示例 | 问题 |
|--------|------|------|
| 空洞废话 | "从而可以更方便"、"从而有更好的体验" | 没有传递任何具体价值 |
| 复述 I want | "从而可以查看净值"（I want 已经说了查看净值） | So that 和 I want 说的是同一件事 |
| 技术导向 | "从而系统性能更好" | 这是技术目标，不是用户价值 |
| 三类都归不进去 | 既不是角色目标，也不是规则流程，也不是旅程衔接 | 上游没梳理清楚，或故事本身不该存在 |

### 第 3 步：INVEST 原则检查

| 原则 | 检查方式 |
|------|---------|
| **I**ndependent（独立） | 这个故事能否独立交付？是否强依赖其他故事先完成？ |
| **N**egotiable（可协商） | 故事是否写得过于具体以至于没有协商空间？是否像合同条款？ |
| **V**aluable（有价值） | So that 是否传递了明确的用户或业务价值？（第 2 步已检查） |
| **E**stimable（可估算） | 团队是否能估算工作量？描述是否足够清晰？ |
| **S**mall（足够小） | 故事是否可以在一个迭代内完成？AC 数量是否暗示故事太大？ |
| **T**estable（可测试） | 是否有验收条件？AC 是否可测试？ |

### 第 4 步：验收条件检查

如果输入中包含 AC，执行以下检查：

#### 4.1 上游可追溯性

逐条 AC 检查：
- **Given** 是否来自用户旅程（用户当前处于什么阶段/状态）
- **When** 是否来自整体解决方案（系统形态、交互方式）
- **Then** 是否来自用户故事本身（要实现的功能价值）

#### 4.2 可测试性

对每条 AC 回答三个问题：
1. **怎么验证？** —— 能否明确描述验证步骤
2. **边界在哪？** —— 是否有量化标准
3. **反例是什么？** —— 能否说清什么情况算不通过

#### 4.3 场景覆盖度

| 路径 | 要求 |
|------|------|
| 正常路径 | 用户按预期操作，一切正常 |
| 边界路径 | 极端值、空数据、首次使用、并发 |
| 异常路径 | 网络异常、权限不足、数据异常 |

#### 4.4 AC 反模式

| 反模式 | 严重程度 |
|--------|---------|
| 含糊形容词（"快速"、"友好"、"合理"） | 严重 |
| 混入实现方案（"用 Redis"、"调 XX 接口"） | 严重 |
| 遗漏主语 | 中等 |
| 缺少量化 | 中等 |
| 复述用户故事 | 严重 |
| Then 不可观测 | 严重 |

#### 4.5 数量检查

| AC 数量 | 判定 |
|---------|------|
| 0 条 | 缺失，必须补充 |
| 1-2 条 | 过少，大概率只有 Happy Path |
| 3-8 条 | 合理 |
| 9+ 条 | 过多，故事需要拆分 |

#### 4.6 边界检查

检查是否有 AC 超出当前故事范围，更适合作为独立故事存在。

### 第 5 步：一致性检查

检查故事卡、So that、AC 三者之间是否一致：
- AC 是否都在 I want 描述的功能范围内
- AC 的 Then 是否都服务于 So that 描述的价值
- 是否有 AC 与故事卡矛盾或偏离

## 输出格式

### 总评

给出评级和一句话概括：

- **A（优秀）**：故事完整、价值清晰、AC 可测试，可直接进入开发
- **B（良好）**：存在少量可改进项，不影响整体理解
- **C（需修改）**：存在明显问题，需修改后才能使用
- **D（需重写）**：根本性问题，建议回到上游重新梳理

### 故事卡审查

```
角色（As a）:    ✅/⚠️/❌ [具体问题]
功能（I want）:  ✅/⚠️/❌ [具体问题]
价值（So that）: ✅/⚠️/❌ [归属类型：角色目标/规则流程/旅程衔接/无法归类]
```

### INVEST 检查

```
Independent:  ✅/⚠️/❌
Negotiable:   ✅/⚠️/❌
Valuable:     ✅/⚠️/❌
Estimable:    ✅/⚠️/❌
Small:        ✅/⚠️/❌
Testable:     ✅/⚠️/❌
```

### AC 逐条审查

```
AC #N: [AC 原文摘要]
├─ 可追溯性: ✅/⚠️/❌ [Given→旅程 / When→方案 / Then→故事]
├─ 可测试性: ✅/⚠️/❌ [能否直接变成测试用例]
├─ 反模式:   ✅/⚠️/❌ [命中了哪些]
└─ 建议:     [具体修改建议]
```

### 覆盖度

```
正常路径: ✅/❌ (N 条)
边界路径: ✅/❌ (N 条)
异常路径: ✅/❌ (N 条)
缺失场景建议: [应补充的具体场景]
```

### 修改建议

按优先级列出：

- **P0（必须修改）**：So that 空洞或缺失、AC 不可测试、故事卡格式不完整
- **P1（建议修改）**：含糊形容词、覆盖度不足、缺少量化
- **P2（可选优化）**：粒度可调整、边界可明确

对每条需修改的内容，给出修改前后的对比。

## 守护栏

- 不替用户编造业务上下文或用户旅程，不确定就问
- 不凭空添加 AC 或修改故事，只在"建议"中提供方向
- 如果缺少上游信息（整体解决方案、用户旅程），明确标注哪些维度无法检查，而非猜测
- 检查基于已有信息，信息不足时标注"无法判断"
- 保持人间清醒，不懂就问
