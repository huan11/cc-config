# 白名单模式：先忽略根下一切，再逐个放行。
# 理由：~/.claude 有 800 MB，其中 projects/（对话记录）、jobs/、history.jsonl
# 都是隐私数据，settings.json 含活的 API token。默认忽略比默认收录安全。

/*

# —— 放行：真正算「配置」的东西 ——
!/.gitignore
!/commands/
!/skills/
!/docs/
!/scripts/
!/CLAUDE.md
!/statusline-command.sh
!/settings.example.json
!/工具空间盘点-2026-09-13.md

# —— 即使在放行目录内，也永不收录 ——
# 可再生的依赖目录（有锁文件，需要时重装）
skills/**/.venv/
skills/**/node_modules/
# 含活 token / 机器专属，只版本化 settings.example.json
/settings.json
/settings.local.json
**/.DS_Store
