从当前对话/迭代中提取新知识，按抽象层级路由到对应的规则文件。

参数：`/absorb [--scope all|rules|skills|todo] [--dry-run]`

| 参数 | 默认 | 说明 |
|------|------|------|
| `--scope` | `all` | `all`=全提取 / `rules`=仅规则 / `skills`=仅检查项 / `todo`=仅待办 |
| `--dry-run` | 关 | 只展示提案，不写入文件（默认也需确认后才写入） |

## 与其他命令的关系

```
/retro   → 生成复盘报告文件（只读分析，不改知识文件）
/todo    → 仅提取未完成任务 → TODO.md
/absorb  → 提取全部知识 → 路由到所有适当文件（包含 /todo 的功能）
/distill → 审计已有文件 → 去重/精简/抽象（存量优化）
```

**典型节奏：** 每次对话结束 `/absorb`，每月一次 `/distill`。

## Phase 1: 扫描对话

回顾当前对话完整历史，提取以下 6 类**知识单元(KU)**：

| KU类型 | 识别方式 | 例子 |
|--------|---------|------|
| **规则** | 发现的应做/不应做 | "正交配置不应互相门控" |
| **陷阱** | 踩过的坑及修复方式 | "str2double对char矩阵返回标量" |
| **模式** | 可复用的解决方案 | "级联修复须重算整条链" |
| **事实** | 环境/配置/数据发现 | "max_turnover是varchar需str2double" |
| **待办** | 提到但未完成的任务 | "H4 partial语义待明确" |
| **技能更新** | 应加入检查清单的新项 | "review.md 应增加 2.11 数据形状变更检查" |

**提取原则：**
- 只提取对话中**实际发生**的，不编造
- 一条 KU 只表达一个知识点
- 保留足够上下文使 KU 独立可理解

## Phase 2: 分级路由

对每条 KU 判定抽象层级，决定目标文件：

| 抽象层级 | 判定标准 | 目标文件 |
|---------|---------|---------|
| **L0 全局铁律** | 适用于任何项目、任何语言 | `~/.claude/CLAUDE.md` |
| **L1 项目规则** | 仅适用于当前项目 | `<project>/CLAUDE.md` |
| **L2 技能检查项** | 属于特定审查/修复领域 | `.claude/commands/review.md` 等 |
| **L3 可复用模式** | 踩坑经验，满足归档条件* | `docs/best-practices.md` |
| **L4 项目事实** | 配置/环境/数据的具体值 | `memory/MEMORY.md` |
| **L5 待办** | 未完成的任务 | `TODO.md` |

*归档条件：同模式≥2次 / 涉及≥3文件 / 反直觉陷阱

**层级判定规则：**
- 问"换一个项目这条规则还适用吗？" → 是=L0，否→继续
- 问"这是一条操作规则还是一个检查项？" → 检查项=L2，规则→继续
- 问"这是一个事实还是一条准则？" → 事实=L4，准则=L1
- 未完成→L5，可复用陷阱→L3

## Phase 3: 去重检查

写入前，对每条 KU 检查目标文件是否已有等价内容：

1. 读取目标文件
2. 搜索语义相近的已有条目
3. 如果已存在：
   - 完全等价 → 跳过（标记"已存在"）
   - 部分重叠 → 提议合并（标记"建议合并"）
   - 新内容是旧内容的扩展 → 提议更新（标记"建议更新"）

## Phase 4: 展示提案

分文件展示所有拟写入的变更：

```
## /absorb 提案

### 本次对话提取 N 条知识单元

#### → ~/.claude/CLAUDE.md (新增 1 条)

  [L0-规则] 同逻辑N副本修一处须全部同步
  + 位置：铁律 > 写代码 > 新增一条
  + 内容：`- 同逻辑多副本（并行SQL路径、多入口函数）修一处须同步全部`

#### → <project>/CLAUDE.md (新增 1 条, 更新 1 条)

  [L1-规则] 新增: boundary cascade 修复须重算整组
  + 位置：并行与缓存 > 新增一条
  + 内容：`- 跨worker边界修复须从修正点串行重算整组，不能只补第一个`

  [L1-规则] 更新: 并行与缓存第5条
  - 旧：`wait(f,'finished',timeout) 不处理 'failed' 状态，须轮询两种终态`
  + 新：`wait(f,'finished',timeout) 不处理 'failed' 状态，须轮询 finished+failed 两种终态`

#### → .claude/commands/review.md (新增 1 检查项)

  [L2-技能] 检查项 3.13: 业务语义边界值
  + 位置：类别3 数据格式 > 末尾新增
  + 内容：`| 3.13 | 0是合法值时不能用 ~=0 判断缺失，应用 ~isnan |`

#### → TODO.md (新增 1 条)

  [L5-待办] H4 partial 状态语义
  + 内容：`- [ ] **H4** partial 状态语义不清 — 需明确 API 契约`

#### 跳过 (2 条已存在)

  [已存在] "除法防零" → CLAUDE.md:27 已有
  [已存在] "parpool后紧跟onCleanup" → CLAUDE.md:24 已有

确认写入？(y/n)
```

## Phase 5: 写入

用户确认后：

1. **按文件分组**，每个文件一次性编辑（减少 I/O）
2. **新增条目**：追加到目标 section 末尾
3. **更新条目**：原位替换
4. **TODO 条目**：按优先级插入对应 section
5. **写入后验证**：重读文件确认格式正确

## Phase 6: 输出摘要

```
## /absorb 完成

已写入:
  ~/.claude/CLAUDE.md      +1 规则
  <project>/CLAUDE.md      +1 规则, ~1 更新
  commands/review.md       +1 检查项
  TODO.md                  +1 待办

跳过: 2 条（已存在）
总计: 4 条新知识已沉淀
```

## 注意事项

- **只提取，不编造** — 每条 KU 必须能在对话中找到来源
- **不替代 /retro** — /retro 生成完整复盘报告（含时间线、错误分析），/absorb 只提取可持久化的知识
- **幂等安全** — 多次运行不会重复写入（Phase 3 去重）
- **最小侵入** — 只追加/更新，不重组已有文件结构（重组是 /distill 的职责）
- **MEMORY.md 特殊处理** — 注意 200 行截断限制，只写入高价值事实，低价值的不写
