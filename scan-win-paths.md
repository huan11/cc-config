---
description: 扫描 Windows 风格路径问题
disable-model-invocation: true
---

扫描项目中所有 Windows 风格路径（反斜杠 `\`、盘符 `C:\`、硬编码绝对路径等），以路径问题解决专家的视角给出完整解决方案。

## 你的角色

你是一名**路径问题解决专家**，精通 Windows / macOS / Linux 三端路径差异，熟悉 Python `pathlib`、`os.path`、Node.js `path`、Java `Path` / `Paths` 等跨平台路径处理方案。你的目标是让项目在任何操作系统上都能正常运行。

## 第一步：全量扫描

在整个项目中搜索以下模式（排除 `.git`、`node_modules`、`__pycache__`、`.venv`、`venv`、`dist`、`build` 目录）：

### 1.1 硬编码 Windows 路径

- 盘符开头：`C:\`、`D:\`、`E:\` 等（含大小写）
- UNC 路径：`\\server\share`、`\\?\` 长路径前缀
- 用户目录硬编码：`C:\Users\xxx`、`%USERPROFILE%`、`%APPDATA%`

### 1.2 反斜杠路径拼接

- 字符串中使用 `\` 作为路径分隔符（注意区分转义字符 `\n`、`\t`、`\\` 等）
- 字符串拼接路径时直接用 `\`：`folder + "\\file.txt"`
- f-string / format 中嵌入反斜杠路径

### 1.3 平台相关 API 调用

- 直接使用 `os.sep` 假定为 `\`
- 使用 `os.path.join` 但参数中混入了 `\`
- `subprocess` / `os.system` 中调用 Windows 专属命令（`dir`、`copy`、`type`）
- 注册表路径操作（`winreg`）

### 1.4 配置文件中的路径

- `.env`、`.yaml`、`.json`、`.toml`、`.ini`、`.xml` 中的 Windows 路径
- Docker 挂载路径中的反斜杠
- CI/CD 配置中的平台特定路径

## 第二步：分类汇总

将扫描结果按严重程度分类：

### 严重程度定义

| 级别 | 含义 | 判定标准 |
|------|------|---------|
| **P0-阻断** | 跨平台必挂 | 硬编码盘符路径、UNC 路径、Windows 专属 API |
| **P1-高危** | 大概率出问题 | 反斜杠拼接、平台假设、用户目录硬编码 |
| **P2-隐患** | 特定场景出问题 | 配置文件中的 Windows 路径、混用分隔符 |
| **P3-建议** | 不影响运行但不规范 | 可以用更好的跨平台 API 替代 |

## 第三步：输出扫描报告

### 3.1 总览

```
扫描文件数：XXX
发现问题数：P0(X) / P1(X) / P2(X) / P3(X)
涉及文件数：XXX
```

### 3.2 问题清单

| 序号 | 级别 | 文件:行号 | 问题代码 | 问题描述 |
|------|------|-----------|----------|----------|
| 1 | P0 | src/config.py:23 | `path = "C:\\data\\output"` | 硬编码 Windows 绝对路径 |
| 2 | P1 | utils/file.py:45 | `folder + "\\file.txt"` | 反斜杠拼接路径 |

### 3.3 问题热力图

按目录统计问题分布，帮助判断哪些模块是重灾区：

```
src/            ████████ 8 个问题
config/         ████ 4 个问题
scripts/        ██ 2 个问题
```

## 第四步：解决方案

对每个问题给出**具体的修复代码**，不是笼统建议。

### 4.1 逐条修复方案

对每个 P0/P1 问题，给出：

```
问题 #1 [P0] src/config.py:23
─────────────────────────────
现状：path = "C:\\data\\output"
修复：path = Path.home() / "data" / "output"
原因：硬编码盘符路径在 macOS/Linux 上不存在
注意：确认业务逻辑是否依赖该绝对路径，如果是用户可配置路径应改为环境变量
```

### 4.2 通用改造建议

根据项目使用的语言，给出对应的跨平台路径最佳实践：

**Python 项目：**
- `pathlib.Path` 替代字符串拼接
- `Path.home()` 替代 `C:\Users\xxx`
- `Path.cwd()` 替代硬编码工作目录
- `tempfile.gettempdir()` 替代 `C:\Temp`

**Node.js 项目：**
- `path.join()` / `path.resolve()` 替代字符串拼接
- `os.homedir()` 替代硬编码用户目录
- `os.tmpdir()` 替代硬编码临时目录
- `process.cwd()` 替代硬编码工作目录

**Java 项目：**
- `Paths.get()` / `Path.of()` 替代字符串拼接
- `System.getProperty("user.home")` 替代硬编码用户目录
- `File.separator` 或 `Path` API 替代 `\\`

**通用：**
- 配置文件中统一使用 `/` 作为路径分隔符（大多数语言的路径 API 在 Windows 上也接受 `/`）
- 环境变量 + 相对路径替代硬编码绝对路径
- `.env` 文件管理平台特定的路径配置

### 4.3 防劣化措施

建议可以落地的防护手段（不强求全部采用）：

1. **lint 规则**：是否可以加自定义 lint 规则检测反斜杠路径
2. **CI 检查**：pre-commit hook 或 CI 中加路径扫描
3. **代码规范补充**：在项目 CONTRIBUTING.md 或 CLAUDE.md 中加路径处理规范

## 第五步：生成修复脚本（可选）

如果问题较多（>10 个），询问用户是否需要生成批量修复脚本。如果用户同意：

- 对 P0/P1 中可以安全自动替换的问题，生成修复脚本
- 对需要人工判断的问题，只标注不自动修复
- 修复前备份原文件

## 输出位置

报告保存到 `docs/path-audit/report.md`（目录不存在则创建）。

## 守护栏

- 扫描结果必须来自实际代码搜索，禁止凭印象编造
- 区分"转义字符"和"路径反斜杠"，`\n`、`\t`、`\r`、`\0` 不是路径问题
- 正则表达式中的 `\` 不是路径问题，需排除
- Windows 注册表路径如果是 Windows 专属功能的合理使用，标记为 P3 而非 P0
- 不确定是否为路径问题的，标注"需人工确认"
- 修复建议必须考虑业务上下文，不能只做机械替换
- 如果项目本身就是 Windows 专属工具，如实说明，不强行跨平台化
