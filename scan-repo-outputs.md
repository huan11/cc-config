---
description: 扫描代码中所有写出操作（文件保存、数据库写入、API 调用等），生成完整的产物清单。回答"这个项目最终产出了什么、写到了哪里"。接手项目、数据血缘追踪、下游影响评估时使用。
---

扫描目标路径，穷举该项目的**全部产物**（输出文件 + 数据库表 + 外发请求），生成产物清单。

目标路径: $ARGUMENTS

如果未提供路径，使用当前工作目录。

## 你的角色

你是一名**产物盘点员**。你的任务是回答一个问题：**这个项目运行完之后，往外面写了什么东西？**

你不关心中间过程，只关心最终落地的产物。

## 扫描维度（3 类产物）

### 1. 文件产物

搜索代码中所有文件写出操作：
- `df.to_csv(...)` / `df.to_excel(...)` / `df.to_parquet(...)`
- `open(..., 'w')` / `open(..., 'a')`
- `json.dump(...)` / `yaml.dump(...)`
- `shutil.copy` / `shutil.move` / `os.rename`
- `plt.savefig(...)` / `fig.savefig(...)`
- `logging.FileHandler(...)` (日志文件也算产物)

对每个写出操作记录：
- **输出路径**：追踪变量拼接还原完整路径（如 `os.path.join(base, 'xxx_' + date + '.csv')`）
- **文件名模式**：如 `index_{YYYYMMDD}.csv`
- **格式**：CSV / Excel / JSON / log / 图片
- **编码**：gbk / utf-8 / 默认
- **写入频率**：每日一个文件 / 覆盖写 / 追加写
- **代码引用**：file:line
- **所属模块**：哪个业务模块产出的

### 2. 数据库产物

搜索代码中所有数据库写入操作：
- `df.to_sql(...)` / `engine.execute(INSERT...)` / `cursor.execute(INSERT...)`
- 项目自定义的 SQL 写入封装（如 `gt.sqlSaving_main()`、`sm.df_to_sql()`）
- `CREATE TABLE` / `ALTER TABLE`

对每个写入操作记录：
- **目标表名**：从函数参数/SQL 语句中提取
- **数据库/schema**：从连接配置推断
- **写入模式**：append / replace / delete+insert / upsert
- **写入频率**：每日 / 按需 / 一次性
- **主要字段**：从 DataFrame 列名或 SQL 字段推断
- **代码引用**：file:line
- **所属模块**

**重要：逻辑名 vs 实际表名**

代码中传给 SQL 写入函数的参数往往是**逻辑名**（如 `sqlSaving_main(config, 'indexData')`），而非数据库中的实际表名。必须追查配置文件（YAML/JSON）中的映射关系，找到 `table_name` 字段对应的**实际表名**（如 `data_index`）。

具体步骤：
1. 从代码中提取逻辑名（`sqlSaving_main` 的第二个参数）
2. 找到 SQL 配置文件（通常是 YAML/JSON，如 `dataUpdate_sql.yaml`）
3. 读取配置，建立 逻辑名 → `table_name` 的映射
4. 同时提取配置中的 `db_url` 字段，确定实际写入哪个数据库
5. **产物清单中必须使用实际表名**，逻辑名作为辅助标注
6. **检测多对一映射**：多个逻辑名可能映射到同一张物理表（如 M1M2/Shibor/CPI 都写入 `data_macro`），此时按物理表去重，合并标注所有逻辑名

### 3. 外发产物

搜索代码中所有对外发送操作：
- `requests.post(...)` / `requests.put(...)`
- Webhook 调用（飞书/钉钉/Slack）
- 邮件发送 (`smtplib` / `email`)
- 消息队列发送 (`kafka` / `rabbitmq` / `redis.publish`)

对每个外发操作记录：
- **目标地址/服务**
- **发送内容**
- **代码引用**

## 输出格式

写入 `docs/repo-outputs-[项目名].md`，结构如下：

```markdown
# 产物清单: [项目名]

> 扫描日期: YYYY-MM-DD
> 扫描路径: [path]

---

## 总览

| 产物类型 | 数量 | 说明 |
|---------|------|------|
| 文件产物 | X 个路径 | CSV / Excel / log |
| 数据库表 | Y 张 | MySQL |
| 外发产物 | Z 个 | Webhook / API |

---

## 一、文件产物

### 按模块分组

#### [模块名]

| # | 输出路径 | 文件名模式 | 格式 | 编码 | 频率 | 代码引用 |
|---|---------|-----------|------|------|------|---------|

<!-- 如果路径通过配置文件定义（如 glv.get('key')），标注 key 名 -->

### 日志文件

| # | 日志路径 | 日志类型 | 滚动策略 | 代码引用 |
|---|---------|---------|---------|---------|

---

## 二、数据库产物

### 按数据库分组

#### [数据库名]

| # | 实际表名 | 逻辑名 (代码中) | 写入模式 | 频率 | 代码引用 | 所属模块 |
|---|---------|---------------|---------|------|---------|---------|

<!-- 实际表名从 SQL 配置文件的 table_name 字段获取，不是代码中的参数名 -->
<!-- 多个逻辑名映射到同一张物理表时，合并为一行，逻辑名用逗号分隔 -->

---

## 三、外发产物

| # | 目标服务 | 协议 | 发送内容 | 代码引用 |
|---|---------|------|---------|---------|

<!-- 如果没有外发产物，标注"无" -->

---

## 四、产物依赖关系（可选）

<!-- 如果某些产物是其他产物的输入（链式依赖），在此标注 -->
<!-- 例如：模块 A 输出 CSV → 模块 B 读取该 CSV → 模块 B 输出到数据库 -->

| 上游产物 | 下游消费者 | 关系 |
|---------|-----------|------|
```

## 执行策略

1. **先搜写出语句** — 用 Grep 搜 `to_csv`、`to_sql`、`to_excel`、`savefig`、`df_to_sql` 等关键模式
2. **路径还原** — 对每个写出语句，向上追踪变量赋值，还原完整输出路径
3. **表名映射** — 搜到数据库写入后，**必须**找到 SQL 配置文件（YAML/JSON），读取 `table_name` 字段获取实际表名，不能直接用代码中的逻辑名。同时检查 `db_url` 确定目标数据库
4. **去重合并** — 同一张物理表/同一个输出路径被多处写入时，合并为一条，标注所有引用位置。特别注意多个逻辑名映射到同一张物理表的情况
5. **按模块分组** — 按业务模块组织，而非按文件名排列
6. **区分配置路径和硬编码路径** — 通过配置获取的路径标注 key 名，硬编码路径直接写出

## 守护栏

- **只写 `docs/repo-outputs-[项目名].md`**，不修改任何已有文件
- **不执行代码** — 纯静态扫描
- **只关注写出操作** — 读取操作不在范围内（那是 `/io-flow` 的工作）
- **中间变量不算产物** — 只有最终落盘/入库/外发的才算
- **产出后展示总览表**，等用户确认
