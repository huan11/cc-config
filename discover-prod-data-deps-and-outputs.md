---
description: 基于已发现的场景，连接生产数据库，搞清楚每个场景读什么数据、写什么数据、现在数据齐不齐。
argument-hint: <数据库连接信息> [目标日期]  例: host:port:user:pass 2026-04-09
---

在 `/discover-prod-scenarios-and-entries` 之后使用。对每个场景回答三个问题：

1. **读什么？** — 依赖哪些输入表
2. **写什么？** — 产出哪些输出表
3. **现在齐不齐？** — 输入有没有、输出有没有、缺了什么

## 前置条件

需要先有场景清单：`docs/prod-scenarios-and-entries-*.md`（`/discover-prod-scenarios-and-entries` 的产出）。
如不存在 → 提示用户先运行 `/discover-prod-scenarios-and-entries`。

## 第一步：确定目标日期

- `$ARGUMENTS` 中如果有日期则使用
- 否则默认今日
- 如果今日非交易日，提示用户，但不自动回退（用户可能就是要查非交易日）

## 第二步：检查输入（每个场景读什么，有没有）

从场景清单或 io-flow.md 中提取每个场景的输入表，连接生产数据库检查：

**按重要性分级**：

| 级别 | 含义 | 缺失后果 |
|---|---|---|
| 必须 | 没有就整个场景跑不了 | 阻塞 |
| 部分必须 | 没有就部分产品/组合跑不了 | 降级 |
| 可选 | 没有只影响个别输出表 | 部分为空 |

对每张输入表：
```sql
SELECT COUNT(*) AS cnt FROM <库名>.<表名> WHERE <日期字段> = '<目标日期>';
```

注意：不同库的日期字段名可能不同，先 `SHOW COLUMNS` 确认。

**如果必须级别的输入缺失 → 在报告中标红，不需要继续查该场景的输出。**

## 第三步：检查输出（每个场景写什么，写了没）

对每张输出表：
```sql
SELECT COUNT(*) AS cnt,
       MIN(update_time) AS min_update,
       MAX(update_time) AS max_update
FROM tracking_new.<表名>
WHERE valuation_date = '<目标日期>';
```

注意：tracking_new 的日期字段是 `valuation_date`。

同时检查：
- **realtime 表**是否还有数据（每天 9:00 清空）
- **history 归档表**当日是否已写入（16:00 后才归档）
- **daily 表**当日是否已写入（通常 23:30 才跑）

## 第四步：输出报告

保存到 `docs/prod-data-deps-and-outputs-<YYYYMMDD-HHmmss>.md`。不覆盖已有文件。

报告结构：

**概览**

> 目标日期: 2026-04-09
> 检查时间: 2026-04-09 16:30
> 数据库: rm-bp1o6we7s3o1h76x1to.mysql.rds.aliyuncs.com:3306

**逐场景报告**（每个场景一节）

> ## 场景一: 实时-组合
>
> ### 输入
> | 级别 | 库.表 | 有数据？ | 行数 |
> |---|---|---|---|
> | 必须 | portfolio_new.portfolio | 有 | 41 |
> | 必须 | rz_hfdb.行情 | 有 | ... |
> | 可选 | data_prepared_new.data_score | 有 | ... |
>
> 输入状态: 全部就绪
>
> ### 输出
> | 表 | 行数 | 最后更新 | 状态 |
> |---|---|---|---|
> | realtime_portfolioreturn | 41 | 15:58 | 正常 |
> | history_portfolioreturn | 41 | 15:30 | 已归档 |
> ...
>
> 输出状态: 全部正常

**汇总**

> | 场景 | 输入 | 输出 | 结论 |
> |---|---|---|---|
> | 实时-组合 | 全部就绪 | 全部正常 | 正常运行 |
> | 实时-产品 | 全部就绪 | 全部正常 | 正常运行 |
> | 每日更新 | 全部就绪 | 4/11 有数据 | 产品级断更 |
> | 数据监控 | - | 飞书告警 | 无法检查 |

## 要求

1. **只执行 SELECT** — 严禁写入
2. **输入先于输出** — 输入缺失的场景不查输出
3. **每张表都要有结论** — 正常 / 缺失 / 断更（附最新日期）/ 未到执行时间
4. **区分"没跑"和"跑了但没数据"** — daily 表 23:30 才跑，16:00 查到空是正常的；但 realtime 表 16:00 查到空是异常的
