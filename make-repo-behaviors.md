---
description: 轻量抽取目标仓库的核心行为和业务规则，生成 repo-behaviors 文档。回答"系统做了什么、遵循什么约束"。接手新项目想快速了解业务逻辑时使用，是 make-* 命令族的第二步。
---

对指定仓库做轻量级行为取证，抽取核心行为（系统做了什么）和业务规则（遵循什么约束），写入目标仓库的 `docs/repo-behaviors.md`。

目标仓库路径: $ARGUMENTS

如果未提供路径，立即询问用户，不要猜测。

## 命令族导航

1. `/make-repo-card` — 这是什么
2. **`/make-repo-behaviors`** ← 当前：做了什么、遵循什么规则
3. `/make-exec-flow` — 怎么跑起来的（宏观执行流）
4. `/make-algo-flow` — 具体怎么算的（微观算法流程）

如果目标仓库还没有 `docs/repo-card.md`，建议用户先跑 `/make-repo-card`，因为本命令会优先读取 repo-card 来快速建立上下文。

## 产出物

`docs/repo-behaviors.md` — 核心行为 + 业务规则文档

## 两个核心概念

**行为（Behavior）**: 系统执行的一条完整链路 — 从哪里拿输入、做了什么处理、输出到哪里。比如"每日从 Wind 拉取行情数据，清洗后写入 MySQL"就是一条行为。

**业务规则（Business Rule）**: 系统在执行行为时遵循的约束和判断逻辑。比如"停牌超过 30 天的股票自动剔除"、"单只股票权重不超过 5%"就是业务规则。规则通常藏在 if 条件、阈值常量、过滤逻辑、校验函数中。

## 执行流程

### Phase 1: 建立上下文

1. 如果目标仓库有 `docs/repo-card.md`，先读它 — 快速了解项目定位、技术栈、入口文件
2. 如果没有 repo-card，先用 `find . -maxdepth 3 -not -path './.git/*' | head -80` 看目录结构，再读 `README.md`、主入口文件

### Phase 2: 追踪核心行为

从主入口出发，沿调用链追踪 3-8 条核心行为。追踪时关注：

- **输入**: 数据从哪来（数据库查询、API 调用、文件读取、用户输入）
- **处理**: 核心转换逻辑是什么（不需要逐行，抓主干）
- **输出**: 结果去了哪（写库、写文件、发消息、返回响应）
- **触发方式**: 定时任务、手动触发、事件驱动、API 请求

### Phase 3: 提取业务规则

在追踪行为的过程中，同步提取业务规则。重点扫描：

- 条件判断（`if`/`else`、`switch`）中的业务逻辑
- 硬编码的阈值、常量、魔数
- 数据过滤和校验逻辑
- 异常处理中的业务分支（不是技术异常，是业务异常）
- 配置文件中的业务参数

每条规则标注它属于哪条行为。

### Phase 4: 生成 repo-behaviors.md

按以下模板填写，写入目标仓库的 `docs/repo-behaviors.md`。

```markdown
# Repo Behaviors: [仓库名]

> 取证日期: YYYY-MM-DD
> 仓库路径: [path]
> 关联卡片: [docs/repo-card.md](repo-card.md)

## 行为总览

| # | 行为名称 | 触发方式 | 输入 | 输出 | 关联规则数 |
|---|---------|---------|------|------|-----------|

## 行为详情

### B-01: [行为名称]

**触发方式:** <!-- 定时/手动/事件驱动/API -->

**输入:**
- 来源:
- 格式:

**处理摘要:**
1. [步骤1]
2. [步骤2]
- 关键代码: `file.py:line`

**输出:**
- 去向:
- 格式:

**关联业务规则:** R-01, R-02

---
<!-- 重复 B-02 ... B-0N -->

## 业务规则清单

| # | 规则描述 | 关联行为 | 代码位置 | 规则类型 |
|---|---------|---------|---------|---------|
| R-01 | | B-01 | `file.py:line` | 过滤/阈值/校验/计算/流程控制 |

## 待确认项
<!-- 追踪过程中不确定的点，诚实标出 -->
```

### Phase 5: 展示确认

产出完成后，向用户展示摘要：

- 识别到 **N 条核心行为**，简要列出每条行为的一句话描述
- 提取了 **M 条业务规则**，按类型统计（过滤 x 条、阈值 x 条、校验 x 条...）
- 如有待确认项，列出

然后告知用户完整文档已写入 `docs/repo-behaviors.md`，等待确认或修改意见。

## 守护栏

- **只抓核心** — 3-8 条主行为足够。这是快速摸底，穷举所有代码路径是 `/biz:extract` 的职责。过度追踪会让产出变成噪音。
- **规则要有出处** — 每条规则必须标注代码位置。没有出处的规则无法验证，后续迁移或重构时会成为隐患。
- **不编造** — 不确定的标"待确认"。宁可少写不要猜，因为错误的规则比遗漏的规则危害更大。
- **目标仓库只读** — 只写 `docs/repo-behaviors.md`，不修改任何已有文件。
- **产出后等确认** — 展示摘要，等用户确认再结束。
